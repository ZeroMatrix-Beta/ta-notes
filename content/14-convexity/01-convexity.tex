% Originally: content/week-06.tex, \section{Convexity} (Corsin Week 6)

% Source: Corsin Nick/Class Notes/Week 6.pdf, p. 2
\section{Convexity}
\label{sec:convexity}

% Sonnet 5 (Medium)
\subsection{Definitions and characterizations}

\begin{definition}[Convex set and convex function]
\label{def:convex_set_function}
A subset $A \subseteq \mathbb{R}^n$ is \newterm{convex} \germanfor{konvex} if for all
$x,y \in A$ and every $t \in [0,1]$, the point $p := (1-t)x+ty$ is an element of $A$.

A function $f : A \to \mathbb{R}$ is convex if $A$ is convex and, for all $x,y \in A$ and
$t \in [0,1]$,
\[f\big((1-t)x+ty\big) \leq (1-t)f(x)+tf(y).\]
\end{definition}

% Sonnet 5 (Medium)
\begin{center}
\begin{tikzpicture}[>=Latex, scale=1.0]
  % Curve is 0.18(r-2.6)^2 + 0.4; the chord endpoints must SIT on it:
  %   f(0.7) = 1.0498,  f(4.6) = 1.12.
  % Interior point at t = 0.35: r = 2.065, chord height 1.0744, curve height 0.4515.
  \draw[->] (-0.3,0) -- (5.6,0);
  \draw[->] (0,-0.3) -- (0,2.0);
  \draw[blue!70!black, thick, domain=0.2:5, samples=60, smooth]
    plot ({\x}, {0.18*(\x-2.6)*(\x-2.6)+0.4});
  \node[blue!70!black, font=\small] at (5.3,1.55) {$f$};

  \draw[orange!90!black, thick] (0.7,1.0498) -- (4.6,1.12);
  \node[orange!90!black, font=\scriptsize] at (3.5,1.32) {chord};
  % Correction: Claude Opus 5 (High) --- this was node[left], and at scale=1.0 an 0.9cm box left
  % of r = 0.7 reaches r = -0.2, so the y axis printed between the "f" and the "(x)". Set above
  % and right of the dot, into the gap between chord and curve: over r = 0.75 to 1.65 the chord
  % stays below y = 1.067 and the curve below y = 1.016, so a box floored at y = 1.18 is clear
  % of both, and it still reads as naming the dot it sits beside.
  \fill (0.7,1.0498) circle (1.5pt);
  \node[font=\small, anchor=south west] at (0.75,1.18) {$f(x)$};
  \fill (4.6,1.12) circle (1.5pt) node[below right, font=\small] {$f(y)$};

  % The convexity inequality, read off vertically at the interior point.
  \draw[gray, dashed] (2.065,0) -- (2.065,1.0744);
  \fill[orange!90!black] (2.065,1.0744) circle (1.5pt);
  \fill[blue!70!black] (2.065,0.4515) circle (1.5pt);
  \draw[red!80!black, <->, thick] (2.065,0.4515) -- (2.065,1.0744);

  \draw (0.7,0.05) -- (0.7,-0.05) node[below, font=\small] {$x$};
  \draw (4.6,0.05) -- (4.6,-0.05) node[below, font=\small] {$y$};
  \draw (2.065,0.05) -- (2.065,-0.05);
  \node[font=\scriptsize, below] at (2.065,-0.12) {$(1-t)x+ty$};
  \node[red!80!black, font=\small] at (2.9,-0.85)
    {$f\big((1-t)x+ty\big) \leq (1-t)f(x)+tf(y)$};
\end{tikzpicture}
\end{center}

% Feedback: Gemini 3.1 Pro (2026-08-11) --- noted as filling a gap the tutor left implicit: the
% convexity characterisations below quantify over semidefiniteness, which was used and never
% defined. Do not fold this back into the proposition that uses it.
\begin{definition}[Semidefinite matrices]
\label{def:semidefinite_matrix}
Let $A \in \mathbb{R}^{n\times n}$ be symmetric. Then $A$ is
\begin{itemize}
  \item \newterm{positive semidefinite} \germanfor{positiv semidefinit}, also called
    \textbf{nonnegative definite}, if $v\transp Av \geq 0$ for all $v \in \mathbb{R}^n$;
  \item \newterm{negative semidefinite} \germanfor{negativ semidefinit}, or
    \textbf{nonpositive definite}, if $v\transp Av \leq 0$ for all $v \in \mathbb{R}^n$.
\end{itemize}
These are the boundary cases of \cref{def:sign_symmetric_matrix}: positive definite implies
positive semidefinite, and the two differ exactly on the degenerate matrices, where
$v\transp Av = 0$ is allowed for some $v \neq 0$.
\end{definition}

% The semidefinite definition above is not in Corsin's notes: he uses the terms freely without
% ever naming them. It is supplied here rather than left implicit. (An AI-Note said so in the
% typeset text until 2026-08-09; it was editor-facing, and it had also gone stale, claiming that
% def:sign_symmetric_matrix comes "later" -- true of the old week order, but that definition now
% sits in chapter 12, i.e. earlier.)

\begin{proposition}[Characterizations of convexity]
\label{prop:convexity_characterizations}
Let $U \subseteq \mathbb{R}^n$ be open and convex, and let $f \in C^2(U)$. Then the following are
equivalent:
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item $f$ is convex;
  \item $\Hess f(x)$ is positive semidefinite for all $x \in U$;
  \item $f(y)-f(x) \geq Df_x(y-x)$ for all $x,y \in U$.
\end{enumerate}
\end{proposition}

\begin{example}[The parabola]
The parabola $p : \mathbb{R}^n \to \mathbb{R}$, $x \mapsto |x|^2 = \sum_{i=1}^n x_i^2$, is convex:
\[\partial_i\partial_j p(x) = 2\delta_{ij} \implies \Hess p(x) = 2\id_n,\]
and every eigenvalue is $2>0$, so $\Hess p(x)$ is positive definite (hence positive semidefinite)
for every $x$, and $p$ is convex by \cref{prop:convexity_characterizations}.
\end{example}

% Supplement: Linus Lüchinger/Slides/Slides-03-23.pdf, slide 17;
%             Linus Lüchinger/Slides/Slides-03-26.pdf, slides 3--4
% Linus's list also contains ||x||^2, which is the preceding example; dropped here.
\begin{exercise}[Convex or not?]
\label{ex:convex_or_not}
Decide which of the following are convex, and prove it. Linus's advice is worth following:
attempt each one \emph{without} plotting it, since that is the situation in an exam. To show a
function \emph{is} convex, computing the Hessian and checking
\cref{prop:convexity_characterizations}\textbf{(b)} is usually quickest; to show one is
\emph{not}, it is usually fastest to exhibit a single triple $x, y, t$ violating the defining
inequality.
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item $f : [0,\infty)^2 \to \mathbb{R}$, $f(x,y) := xy$.
  \item $f : \mathbb{R}^2 \to \mathbb{R}$, $f(x,y) := x^2+xy+y^2$.
  \item $f : [0,\pi]^2 \to \mathbb{R}$, $f(x,y) := \cos(x)\cos(y)$.
  \item If $g, h : \mathbb{R} \to \mathbb{R}$ are convex, is $(x,y) \mapsto g(x)h(y)$ necessarily
    convex?
\end{enumerate}
\exsol{sol:convex_or_not}
\end{exercise}

% Sonnet 5 (Medium)
\subsection{Proof of the characterizations}

% Source: Corsin Nick/Class Notes/Week 6.pdf, pp. 3--6
\begin{exercise}[Equivalent characterizations of convexity]
\label{ex:convexity_characterizations}
Let $U \subseteq \mathbb{R}^n$ be open and convex.
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item Show that $f : U \to \mathbb{R}$ is convex if and only if the functions
    \[f_{x,y} : [0,1] \to \mathbb{R}, \qquad s \mapsto f(x+s(y-x))\]
    are convex for all $x,y \in U$.
  \item Show that
    \[f \text{ convex} \iff f(y)-f(x) \geq Df_x(y-x) \quad \text{for all } x,y \in U.\]
    This also holds for $f \in C^1(U)$, $U \subseteq \mathbb{R}^n$ open and convex.
  \item Conclude that if $f \in C^1(U)$ is convex and has a critical point at $x_0 \in U$, then $f$
    has a minimum at $x_0$.
\end{enumerate}
\end{exercise}

% Kept inline: solved directly in Corsin Nick/Class Notes/Week 6.pdf, pp. 3--6.
\begin{exercisesolution}[Solution to \cref{ex:convexity_characterizations}]
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item By definition, $f_{x,y}$ is convex if and only if
    \[f_{x,y}\big((1-t)s_0+ts_1\big) \leq (1-t)f_{x,y}(s_0)+tf_{x,y}(s_1) \quad \text{for all } s_0,s_1,t \in [0,1],\]
    which, unwinding the definition of $f_{x,y}$, reads
    \[f\big(x+[(1-t)s_0+ts_1](y-x)\big) \leq (1-t)f(x+s_0(y-x))+tf(x+s_1(y-x)),\]
    that is,
    \[f\big((1-t)(x+s_0(y-x))+t(x+s_1(y-x))\big) \leq (1-t)f(x+s_0(y-x))+tf(x+s_1(y-x)).\]
    Setting $s_0=0$, $s_1=1$ recovers exactly the convexity inequality for $f$ at the points $x$
    and $y$, so if this holds for all $x,y \in U$, then $f$ is convex. Conversely, if $f$ is
    convex, then convexity of $f_{x,y}$ for each fixed $x,y \in U$ follows by applying convexity
    of $f$ to the two points $x+s_0(y-x)$ and $x+s_1(y-x)$, which both lie in $U$ since $U$ is
    convex.
  \item Recall that $g \in C^1(I)$, for $I \subseteq \mathbb{R}$ an interval, is convex if and
    only if $g'$ is increasing. Combined with \textbf{(a)}, this gives
    \[f \text{ convex} \iff f'_{x,y} \text{ is increasing, for all } x,y \in U.\]
    By the chain rule, for $t \in [0,1]$,
    \[f'_{x,y}(t) = Df_{x+t(y-x)}(y-x),\]
    and by the mean value theorem there exists $s \in (0,1)$ such that
    \[f(y)-f(x) = f_{x,y}(1)-f_{x,y}(0) = f'_{x,y}(s) = Df_{x+s(y-x)}(y-x).\]

    \textbf{($\Rightarrow$)} If $f$ is convex, then $f_{x,y}$ is convex for all $x,y \in U$ by
    \textbf{(a)}, so $f'_{x,y}$ is increasing, and in particular $f'_{x,y}(s) \geq f'_{x,y}(0)$.
    Combined with the mean value theorem identity above, this yields
    \[f(y)-f(x) \geq Df_x(y-x).\]

    \textbf{($\Leftarrow$)} Suppose $f(y)-f(x) \geq Df_x(y-x)$ for all $x,y \in U$. We must show
    that $0 \leq s < t \leq 1 \implies f'_{x,y}(s) \leq f'_{x,y}(t)$. Set $u := x+s(y-x)$ and
    $v := x+t(y-x)$, so that $u-v = (s-t)(y-x)$. Applying the hypothesis to the pairs $(u,v)$ and
    $(v,u)$ gives
    \[Df_u(v-u) \leq f(v)-f(u), \qquad Df_v(u-v) \leq f(u)-f(v).\]
    Therefore,
    \[(t-s)f'_{x,y}(s) = Df_u(v-u) \leq f(v)-f(u) \leq -Df_v(u-v) = Df_v(v-u) = (t-s)f'_{x,y}(t),\]
    and since $t-s>0$, this gives $f'_{x,y}(s) \leq f'_{x,y}(t)$, i.e., $f'_{x,y}$ is increasing,
    hence $f_{x,y}$ is convex for all $x,y \in U$, and so $f$ is convex by \textbf{(a)}.

    \begin{remark}
    The argument above only ever used differentiability of $f$, never twice-differentiability, so
    the equivalence in \textbf{(b)} already holds for $f \in C^1(U)$ with $U \subseteq
    \mathbb{R}^n$ open and convex, one order of regularity weaker than
    \Cref{prop:convexity_characterizations}, which is stated for $f \in C^2(U)$. This is exactly
    the fact used in \cref{ex:convexity_properties_multiple_choice}\textbf{(b)}.
    \end{remark}
  \item Since $f$ is convex and $x_0 \in U$ is a critical point, $Df_{x_0} = 0$, so \textbf{(b)}
    gives
    \[0 = Df_{x_0}(y-x_0) \leq f(y)-f(x_0) \quad \text{for all } y \in U,\]
    i.e., $f(y) \geq f(x_0)$ for all $y \in U$. Hence $f$ has a minimum at $x_0$, and in fact a
    global minimum on $U$. In the convex case this sharpens
    \cref{prop:extremum_implies_critical}, which on its own only guarantees that critical points
    are the sole \emph{candidates} for extrema.
\end{enumerate}
\end{exercisesolution}

% Sonnet 5 (Medium) --- FIG-W06-01, drawn from the page image seen while transcribing
\begin{center}
\begin{tikzpicture}[>=Latex, scale=1.15]
  \draw[->] (-0.3,0) -- (5.2,0) node[right, font=\small] {$r$};
  \draw[->] (0,-0.3) -- (0,3.0);

  \draw[green!60!black, thick, domain=0.2:5.0, samples=60, smooth]
    plot ({\x}, {0.20*(\x-2.6)*(\x-2.6) + 1.0});
  % Correction: Claude Opus 5 (High) --- this label was centred at (5.35,2.2), so its box ran
  % from r = 4.80 to 5.90 at heights 2.09 to 2.31, and the curve reaches 2.09 at r = 4.94 and
  % ends at (5.0,2.152): the last stretch of the graph printed through its own name. Set above
  % the end of the plot rather than beside it, which also keeps it inside the axis.
  \node[green!60!black, font=\small, anchor=south east] at (5.0,2.28) {$f_{x,y}(r)$};

  % True tangents to f_{x,y}(r) = 0.20(r-2.6)^2 + 1.0:
  %   at s = 1.2: value 1.392, slope -0.56;   at t = 3.6: value 1.200, slope +0.40.
  % They must touch the curve and lie below it -- that is what part (b) says.
  \draw[orange!90!black, thick, domain=0.3:3.0]
    plot ({\x}, {-0.56*(\x-1.2) + 1.392});
  \draw[purple, thick, domain=1.8:5.0]
    plot ({\x}, {0.40*(\x-3.6) + 1.200});

  \fill[orange!90!black] (1.2,1.392) circle (1.6pt);
  \fill[purple] (3.6,1.200) circle (1.6pt);

  % Correction: Claude Opus 5 (High) --- both slope labels were single lines about 3.8cm wide.
  % Node text does not follow the picture's scale=1.15, so each box was 3.30 units across --
  % two thirds of the whole plot. LabU, centred at r = 1.45, reached r = -0.23 and the y axis
  % printed between the "s" and the "lope" of "slope"; LabV, centred at 4.15, reached back to
  % r = 2.50 and the orange tangent line ran through the word "slope" of the label naming the
  % OTHER tangent. Both boxes also drove the picture wider than its own axes.
  %
  % Broken over two lines instead of shifted sideways: that halves each box to about 1.74 units,
  % so LabU spans r = 0.58 to 2.32 (clear of the axis at 0) and LabV spans 3.73 to 5.47 (clear
  % of the dashed guide at t = 3.6, and past it the orange tangent has already stopped at 3.0).
  % `align=center' was already set on both and was doing nothing without a line break.
  \node[orange!90!black, font=\scriptsize, align=center] (LabU) at (1.45,2.45)
    {slope $f'_{x,y}(s)$\\ $= Df_u(y-x) < 0$};
  \draw[orange!90!black, thin, ->] (LabU) -- (1.2,1.47);
  % At 4.6 the box's left edge landed within 0.03cm of the dashed guide at t = 3.6, so the
  % guide's lowest dash read as part of the "=" beginning the second line. 4.75 opens that to
  % about 0.2cm, at a cost of 0.17cm of extra width -- the sprawl this figure had was the
  % 3.3-unit label boxes, not a fraction of a millimetre here.
  \node[purple, font=\scriptsize, align=center] (LabV) at (4.75,0.42)
    {slope $f'_{x,y}(t)$\\ $= Df_v(y-x) > 0$};
  \draw[purple, thin, ->] (LabV) -- (3.6,1.12);

  % Correction: Claude Opus 5 (High) --- this was node[below] on a tick at x = 0, so the label
  % was centred on x = 0 and the y axis, which runs down to y = -0.3, printed straight through
  % the glyph: the zero came out struck through. Set below and left of the origin, where both
  % axes are clear of it. The other three ticks are away from the axis and stay as they are.
  \draw (0,0.05) -- (0,-0.05);
  \node[font=\small, anchor=north east] at (-0.08,-0.06) {$0$};
  \draw (1.2,0.05) -- (1.2,-0.05) node[below, font=\small] {$s$};
  \draw (3.6,0.05) -- (3.6,-0.05) node[below, font=\small] {$t$};
  \draw (4.9,0.05) -- (4.9,-0.05) node[below, font=\small] {$1$};
  \draw[dashed, gray] (1.2,0) -- (1.2,1.392);
  \draw[dashed, gray] (3.6,0) -- (3.6,1.200);
\end{tikzpicture}
\end{center}

The picture records both halves of \textbf{(b)} at once. Each tangent line lies weakly
\emph{below} the graph, which is the inequality $f(y)-f(x) \geq Df_x(y-x)$ read along the
segment, and the slope grows from $s$ to $t$, which is $f'_{x,y}$ increasing.

% Supplement: Analysis_II_Script_v1.pdf, p. 75
\begin{proposition}[Jensen's inequality]
\label{prop:jensen_inequality}
Let $U \subseteq \mathbb{R}^n$ be open and convex and $f : U \to \mathbb{R}$ convex. Then for
every finite collection of points $x_1,\dots,x_N \in U$ and weights $w_1,\dots,w_N \in (0,1)$
with $\sum_{i=1}^N w_i = 1$,
\[f\Big(\sum_{i=1}^N w_ix_i\Big) \ \leq\ \sum_{i=1}^N w_if(x_i).\]
\end{proposition}

\begin{proof}
Let $z := \sum_i w_ix_i \in U$ ($U$ is convex, so this weighted average lies in $U$). By
\cref{ex:convexity_characterizations}\textbf{(b)} (valid already for $f \in C^1$), applied to the
pair $(z,x_i)$ for each $i$,
\[f(x_i) - f(z) \geq Df_z(x_i-z), \qquad i=1,\dots,N.\]
Multiply the $i$-th inequality by $w_i>0$ and sum over $i$:
\begin{align*}
\sum_i w_if(x_i) - f(z) &= \sum_i w_i\big(f(x_i)-f(z)\big) \\
  &\geq Df_z\Big(\sum_i w_i(x_i-z)\Big) = Df_z\Big(\sum_iw_ix_i - z\Big) = Df_z(0) = 0,
\end{align*}
using $\sum_iw_i=1$ to pull $f(z)$ out on the left and $\sum_iw_ix_i=z$ in the last step.
Rearranging gives $f(z) \leq \sum_iw_if(x_i)$, which is the claim. (For $f$ merely convex,
without $C^1$ regularity, the same computation goes through with
\cref{ex:convexity_characterizations}\textbf{(b)} replaced by the general subgradient
inequality, which holds for any convex function on an open set.)
\end{proof}

\begin{remark}
The case $N=2$ is exactly \cref{def:convex_set_function} itself: Jensen's inequality is the
statement that convexity, defined only for a two-point average, automatically extends to any
finite weighted average. It is used silently in the solution to \cref{ex:p_means_monotonicity}
(\cref{ch:metric_spaces}), applied to $\varphi(s):=s\log s$, and is stated explicitly here for
the first time.
\end{remark}

% Generator: Claude Opus 5 (High) --- part \textbf{(c)} of the exercise above says a critical
% point of a convex function is a global minimum. It is worth pairing that with the fact that
% convexity does not produce a critical point in the first place.
% Feedback: Gemini 3.1 Pro (2026-08-11) --- singled out for clearing up a very common
% misconception: convex plus bounded below does not give a minimum, because compactness is
% missing. Keep it next to the characterisation it qualifies.
\begin{example}[Convex, bounded below, and yet no minimum]
\label{ex:convex_function_unattained_infimum}
\Cref{ex:convexity_characterizations}\textbf{(c)} guarantees that \emph{if} a convex function has
a critical point, that point is a global minimum. It does not guarantee that one exists. Take
\[f : \mathbb{R}^2 \to \mathbb{R}, \qquad f(x_1,x_2) := e^{x_1+x_2}.\]
Its Hessian is
\[\Hess f(x_1,x_2) = e^{x_1+x_2}\begin{pmatrix} 1 & 1 \\ 1 & 1\end{pmatrix},\]
so for every $v \in \mathbb{R}^2$ we get
$v\transp\Hess f(x)\,v = e^{x_1+x_2}(v_1+v_2)^2 \geq 0$. The Hessian is positive semidefinite
everywhere, so $f$ is convex by \cref{prop:convexity_characterizations}. But $f > 0$ everywhere,
while along the ray $(-t,-t)$ we have $f(-t,-t) = e^{-2t} \to 0$ as $t \to \infty$. Hence
$\inf_{\mathbb{R}^2}f = 0$ and the infimum is attained nowhere.

What fails is not convexity but compactness: the sublevel sets $\{f \leq c\}$ are unbounded, so
the extreme value theorem (\cref{lem:sequential_compactness_extreme_values}) has nothing to act
on. Convexity organises the minima a function has. It does not manufacture one.
\end{example}

% Transition: Claude Opus 5 (High) --- the paragraph that stood here announced the inverse
% function theorem, which the old week-based structure placed next; that job now belongs to the
% opener of Part V. Replaced with a close to the convexity material itself.
Convexity is worth the trouble because of what it removes. Under it there is no saddle to rule
out, no degenerate case in which the second-order test falls silent, and no gap left between a
local and a global minimum. What it does not remove is the need for a minimum to exist at all,
which is the one thing \cref{ex:convex_function_unattained_infimum} was there to show.


% --- exercises relocated here from the dissolved sheets ---

% Originally: content/week-06.tex, \exercisesheet{6}, problem 6.7
% Source: exercises/Ex6_Analysis2_eng.pdf

\begin{exercise}[Multiple choice (properties of convex functions) \textnormal{(important)}]
\label{ex:convexity_properties_multiple_choice}
Among the following statements about convex functions mark those (and only those) which are
always true.
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item If $f \in C^1(U)$ is convex in some open convex set $U \subset \mathbb{R}^n$ and $f$ has a
    local maximum at $z \in U$, then $\nabla f \equiv 0$ in $U$.
  \item If $f \in C^1(U)$ is convex in some open convex set $U \subset \mathbb{R}^n$ and $f$ has a
    global maximum at $z \in U$, then $\nabla f \equiv 0$ in $U$.
  \item Assume $f_n \in C^2(\mathbb{R})$ is a sequence of convex functions that converge
    pointwise to some $f : \mathbb{R} \to \mathbb{R}$. Is $f$ necessarily convex?
  \item There exists a convex function $f \in C^\infty(\mathbb{R}^2)$ such that
    \[f(x) = 1 - 2x_1 + x_2^3 + O(|x|^4) \quad \text{as } |x| \to 0.\]
  \item There exists a convex function $f \in C^\infty(\mathbb{R}^2)$ such that
    \[f(x) = 1 - 2x_1 + x_2^4 + O(|x|^4) \quad \text{as } |x| \to 0.\]
  \item A convex set is not necessarily connected.
\end{enumerate}
\exinfo{This exercise is Problem 6.7 of Problem Sheet 6, Analysis II, Spring Semester 2026.}
\exsol{sol:convexity_properties_multiple_choice}
\end{exercise}
