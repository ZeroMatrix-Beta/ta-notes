% Originally: content/week-10.tex, \section{Vector calculus operators} (Corsin Week 10)

\section{Vector calculus operators}
\label{sec:vector_calculus_operators}

\begin{definition}[Vector field]
\label{def:vector_field}
Let $U \subseteq \mathbb{R}^n$ be open. A \newterm{$C^k$ vector field} \germanfor{Vektorfeld} is a function $F : U \to \mathbb{R}^n$ of class $C^k(U,\mathbb{R}^n)$.
The scalar functions $F_1, \dots, F_n$ in $F(x) = (F_1(x), \dots, F_n(x))\transp$ are called the \newterm{components}.
\end{definition}

\begin{definition}[Gradient]
\label{def:gradient_vector_field}
Let $f : U \to \mathbb{R}$ be a function of class $C^k$ on an open set $U \subseteq \mathbb{R}^n$. The \newterm{gradient} of $f$ is the $C^{k-1}$ vector field
\[\operatorname{grad} f(x) = \nabla f(x) := \begin{pmatrix} \partial_1 f(x) \\ \vdots \\ \partial_n f(x) \end{pmatrix}.\]
\end{definition}

\begin{definition}[Divergence]
\label{def:divergence}
For a $C^1$ vector field $F : U \to \mathbb{R}^n$, the \newterm{divergence} is the scalar function
\[\divg F(x) := \sum_{i=1}^n \partial_i F_i(x).\]
In 3D, physicists write this as $\vec{\nabla} \cdot \vec{F}(x) = \partial_1 F_1(x) + \partial_2 F_2(x) + \partial_3 F_3(x)$.
\end{definition}

% Generator: Claude Opus 5 (High) --- FIG-CH22-00. The divergence and the curl each had a
% picture in this section and the gradient had none, although it is the one of the three whose
% picture a reader can check against a formula they already know.
%
% GEOMETRY, checked. Level curves of f(x,y) := x^2/4 + y^2 at c = 0.25, 1, 2.25, i.e. ellipses
% with semi-axes (2 sqrt c, sqrt c) = (1, 0.5), (2, 1) and (3, 1.5). Gradient grad f = (x/2, 2y).
% The eight sample points all sit on the middle ellipse c = 1:
%   (+-2, 0)                 -> grad = (+-1, 0),            length 1
%   (0, +-1)                 -> grad = (0, +-2),            length 2
%   (+-sqrt2, +-sqrt(1/2))   -> grad = (+-sqrt(1/2), +-sqrt2), length sqrt(2.5) = 1.581
% (Check the last point is on the ellipse: 2/4 + 1/2 = 1.) Arrows are drawn at 0.45 x the true
% gradient, so their lengths are 0.45, 0.90 and 0.71.
%
% The figure is worth having because two facts are visible in it and neither is visible in the
% definition. PERPENDICULARITY: the tangent to x^2/4 + y^2 = 1 at (sqrt2, sqrt(1/2)) has
% direction (2y, -x/2) = (sqrt2, -sqrt(1/2)), and <(sqrt(1/2), sqrt2), (sqrt2, -sqrt(1/2))>
% = 1 - 1 = 0. STEEPNESS: from c = 1 to c = 2.25 the level curves are 3 - 2 = 1 apart along the
% x-axis and 1.5 - 1 = 0.5 apart along the y-axis, exactly half; and the gradient there is
% exactly twice as long. The arrow lengths encode the spacing rather than merely decorating it.
\begin{remark}[The gradient, read off the level curves]
\label{rem:gradient_and_level_curves}
Two properties of $\nabla f$ are easier to see than to prove, and a contour picture shows both at
once. The gradient at a point is \textbf{perpendicular to the level curve} through that point,
and its \textbf{length measures how tightly the level curves are packed} there.
\begin{center}
\begin{tikzpicture}[>=Latex, scale=1.15]
  % Level curves of f = x^2/4 + y^2 at c = 0.25, 1, 2.25.
  \draw[gray!45] (0,0) ellipse (1 and 0.5);
  \draw[gray!45] (0,0) ellipse (3 and 1.5);
  \draw[MidnightBlue!70, thick] (0,0) ellipse (2 and 1);
  % Label placement is constrained: the eight gradient arrows occupy the axes and the
  % diagonals, so the only free zones are beyond the left and right arrows and directly
  % above and below the top and bottom ones. A first version put "$f=1$" at (2.05,-0.30)
  % and it was overprinted by the red label at (2.42,-0.02). The numeric level values were
  % carrying nothing the figure needs, so they are gone and one caption names the curves.
  \node[gray!55!black, font=\scriptsize, anchor=west] at (2.0,-1.18) {level curves of $f$};

  % Gradients at eight points of the middle level curve, at 0.45x true length.
  \foreach \px/\py/\gx/\gy in {%
      2/0/0.45/0, -2/0/-0.45/0, 0/1/0/0.9, 0/-1/0/-0.9,
      1.414/0.707/0.318/0.636, -1.414/0.707/-0.318/0.636,
      1.414/-0.707/0.318/-0.636, -1.414/-0.707/-0.318/-0.636} {
    \draw[BrickRed, thick, ->] (\px,\py) -- ++(\gx,\gy);
    \fill[MidnightBlue!70] (\px,\py) circle (1.1pt);
  }
  \node[BrickRed, font=\scriptsize, anchor=south] at (0,1.98) {$\nabla f$ is longest here};
  \node[BrickRed, font=\scriptsize, anchor=west] at (2.52,0) {and shortest here};
\end{tikzpicture}
\end{center}
Both readings are the same fact. Moving a distance $\varepsilon$ in the direction $\nabla f$
changes $f$ by about $\varepsilon\lvert\nabla f\rvert$, so where $\lvert\nabla f\rvert$ is large
one crosses from one level curve to the next after a short step, and where it is small one has
further to walk. Moving \emph{along} a level curve does not change $f$ at all, so the directional
derivative vanishes there, which says exactly that $\nabla f$ is orthogonal to the curve.
\end{remark}

\begin{remark}[Sources and sinks]
Intuitively, $\divg F(x)$ tells us how much flow of $F$ is generated at $x$. If $\divg F(x) = 0$, then the total generated flow is zero.
If $\divg F(x) > 0$, then $x$ is a \newterm{source} \germanfor{Quelle}, meaning more flow is generated than removed.
If $\divg F(x) < 0$, then $x$ is a \newterm{sink} \germanfor{Senke}, meaning more flow is removed than generated.

% Supplement: Sascha Brack/Class Notes/Week_10_Notes_Monday.pdf, p. 9
% Revised 2026-08-09. Two faults in the drawing it replaces, both visible only once rendered:
%   * Each caption was attached with `label=above:` to the node at the CENTRE of its star, so
%     it was typeset on top of the arrows rather than clear of them. Both captions ran straight
%     through their own diagram. They are now anchored below the whole figure.
%   * The sink was drawn as `<-` from the centre outward, putting all eight arrowheads at one
%     point; they overlapped into an unreadable blob. The rays now run inward and STOP at
%     radius 0.30, so the eight heads land distinctly on a small circle.
% The two stars are otherwise identical, which is the point: same rays, opposite direction.
\begin{center}
\begin{tikzpicture}[>=Latex, scale=1]
  \foreach \a in {0,45,...,315}
    \draw[blue!80!black, thick, ->] (\a:1.0) -- (\a:0.30);
  \fill[blue!60] (0,0) circle (2.2pt);
  \node[font=\small, anchor=north] at (0,-1.32) {sink: $\divg F < 0$};

  \begin{scope}[shift={(4.2,0)}]
    \foreach \a in {0,45,...,315}
      \draw[red!85!black, thick, ->] (\a:0.30) -- (\a:1.0);
    \fill[red!70] (0,0) circle (2.2pt);
    \node[font=\small, anchor=north] at (0,-1.32) {source: $\divg F > 0$};
  \end{scope}
\end{tikzpicture}
\end{center}
\end{remark}

\begin{definition}[Rotation/Curl]
\label{def:rotation}
Let $F$ be a $C^k$ vector field on an open set $U \subseteq \mathbb{R}^3$. The \newterm{rotation} or \newterm{curl} of $F$ is the $C^{k-1}$ vector field
\[\curl F(x) := \begin{pmatrix} \partial_2 F_3(x) - \partial_3 F_2(x) \\ \partial_3 F_1(x) - \partial_1 F_3(x) \\ \partial_1 F_2(x) - \partial_2 F_1(x) \end{pmatrix}.\]
Physicists write $\curl F(x) = \vec{\nabla} \times \vec{F}(x)$.
In $\mathbb{R}^2$, the rotation of a vector field $F = (F_1, F_2)\transp$ is the scalar function $\curl F(x) := \partial_1 F_2(x) - \partial_2 F_1(x)$.
\end{definition}

\begin{remark}[The paddle-wheel reading of the curl]
Intuitively, $\curl F(x)$ tells us how much the field tends to rotate around $x$. If $\curl F(x) \neq 0$, a small wheel centered at $x$ would rotate.

% Supplement: Sascha Brack/Class Notes/Week_10_Notes_Monday.pdf, p. 10
% Revised 2026-08-09, and one of the changes is a correction, not a cosmetic one.
%
%   * SENSE OF ROTATION. The shear field on the left is $F = (c + ky,\,0)$ with $k>0$, since the
%     arrows get longer as $y$ increases. Then $\curl F = \partial_1F_2 - \partial_2F_1 = -k < 0$,
%     so a paddle wheel turns CLOCKWISE: its top sits in faster rightward flow than its bottom.
%     The old figure drew `arc (-90:90:0.2)`, which sweeps through increasing angles, i.e.
%     COUNTERCLOCKWISE. The arc below runs 120 -> -150, decreasing, which is clockwise.
%   * The old right-hand panel carried a vertical blue arrow from (3.75,-0.6) to (3.75,-0.2)
%     that depicted nothing; it read as an arrow pointing at its own caption. Dropped.
%   * Both indicators were floating next to the field rather than in it. Each panel now shows
%     an actual paddle wheel sitting in the flow, drawn last so the arrows pass behind it.
\begin{center}
\begin{tikzpicture}[>=Latex, scale=1.0]
  % ---- left: shear. Arrow length grows with y, so the wheel is spun clockwise. ----
  \begin{scope}
    \foreach \y/\len in {0.6/1.85, 0.3/1.55, 0/1.25, -0.3/0.95, -0.6/0.65}
      \draw[gray!80!black, thick, ->] (0,\y) -- (\len,\y);
    \fill[white] (0.95,0) circle (0.26);
    \draw[blue!70!black, thick] (0.95,0) circle (0.26);
    \foreach \a in {0,90,180,270}
      \draw[blue!70!black, thick] (0.95,0) -- ++(\a:0.26);
    \draw[blue!70!black, very thick, ->]
      (0.95,0) ++(120:0.46) arc[start angle=120, end angle=-150, radius=0.46];
    \node[blue!70!black, font=\small, anchor=north] at (0.95,-1.05) {rotates};
  \end{scope}

  % ---- right: uniform field, every arrow the same length, so no shear and no rotation ----
  \begin{scope}[shift={(4.0,0)}]
    \foreach \y in {0.6,0.3,0,-0.3,-0.6}
      \draw[gray!80!black, thick, ->] (0,\y) -- (1.25,\y);
    \fill[white] (0.95,0) circle (0.26);
    \draw[blue!70!black, thick] (0.95,0) circle (0.26);
    \foreach \a in {0,90,180,270}
      \draw[blue!70!black, thick] (0.95,0) -- ++(\a:0.26);
    \node[blue!70!black, font=\small, anchor=north] at (0.95,-1.05) {does not rotate};
  \end{scope}
\end{tikzpicture}
\end{center}
\end{remark}

\begin{example}[Computing vector calculus operators]
\label{ex:compute_operators}
Given $\phi : \mathbb{R}^3 \to \mathbb{R}$, $\phi(x,y,z) := x^2 + y^2 + z^2 e^{-xy}$.
Let us compute the gradient, the divergence of the gradient, and the rotation of the gradient.
\begin{itemize}
    \item \textbf{Gradient:}
    \[\nabla \phi(x,y,z) = \begin{pmatrix} 2x - yz^2 e^{-xy} \\ 2y - xz^2 e^{-xy} \\ 2z e^{-xy} \end{pmatrix}.\]
    
    \item \textbf{Divergence of the gradient} (denoted $\divg(\nabla \phi)$ or $\Delta \phi$):
    \begin{align*}
    \divg(\nabla \phi) &= \partial_x(2x - yz^2 e^{-xy}) + \partial_y(2y - xz^2 e^{-xy}) + \partial_z(2z e^{-xy}) \\
    &= (2 + y^2 z^2 e^{-xy}) + (2 + x^2 z^2 e^{-xy}) + (2 e^{-xy}) \\
    &= 4 + (x^2 z^2 + y^2 z^2 + 2) e^{-xy}.
    \end{align*}
    
    \item \textbf{Rotation of the gradient} ($\curl(\nabla \phi)$):
    \begin{align*}
    \curl(\nabla \phi) &= \begin{pmatrix} \partial_y(2z e^{-xy}) - \partial_z(2y - xz^2 e^{-xy}) \\ \partial_z(2x - yz^2 e^{-xy}) - \partial_x(2z e^{-xy}) \\ \partial_x(2y - xz^2 e^{-xy}) - \partial_y(2x - yz^2 e^{-xy}) \end{pmatrix} \\
    &= \begin{pmatrix} -2xz e^{-xy} - (-2xz e^{-xy}) \\ -2yz e^{-xy} - (-2yz e^{-xy}) \\ -z^2 e^{-xy} - (-z^2 e^{-xy}) \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \\ 0 \end{pmatrix}.
    \end{align*}
    This is not a coincidence: for any $C^2$ function $f$, $\curl(\nabla f) = 0$ by Schwarz's theorem.
\end{itemize}
\end{example}

% Supplement: Analysis_II_Script_v1.pdf, p. 163
\begin{exercise}[$\curl\circ\grad=0$ and $\divg\circ\curl=0$]
\label{ex:curl_grad_div_curl_zero}
Let $f$ be a twice continuously differentiable function and $F$ a twice continuously
differentiable vector field on an open set $U \subseteq \mathbb{R}^3$. Show that
\[\curl(\grad f) = 0 \qquad\text{and}\qquad \divg(\curl F) = 0.\]
\end{exercise}

\begin{exercisesolution}[Proof of \cref{ex:curl_grad_div_curl_zero}]
% Generator: Claude Sonnet 5 (High)
Both identities follow from Schwarz's theorem (\cref{thm:schwarz_mixed_partials}), which
applies since $f,F \in C^2$.

\textbf{$\curl(\grad f)=0$.} The first component of $\curl(\grad f)$ is
$\partial_2(\partial_3f) - \partial_3(\partial_2f) = 0$ by Schwarz, and the other two
components vanish the same way, cyclically permuting the indices. This is exactly
\cref{ex:compute_operators}'s observation above, now stated and proved in general.

\textbf{$\divg(\curl F)=0$.}
\[\divg(\curl F) = \partial_1(\partial_2F_3-\partial_3F_2) + \partial_2(\partial_3F_1-\partial_1F_3)
  + \partial_3(\partial_1F_2-\partial_2F_1),\]
and every one of the six terms cancels against another by Schwarz: $\partial_1\partial_2F_3$
cancels $-\partial_2\partial_1F_3$, $-\partial_1\partial_3F_2$ cancels
$\partial_3\partial_1F_2$, and $\partial_2\partial_3F_1$ cancels $-\partial_3\partial_2F_1$.
\end{exercisesolution}

\begin{remark}
This is the classical-vector-calculus shadow of \qt{$d^2=0$}
(\cref{thm:exterior_derivative}\textbf{(2)}, \cref{ch:differential_forms}): $\grad$, $\curl$ and
$\divg$ are exactly the $0$-, $1$- and $2$-form incarnations of the exterior derivative in
$\mathbb{R}^3$ (\cref{ex:exterior_derivative_gradient_curl}), and both identities above are the
single fact $d\circ d=0$ read off once in each degree.
\end{remark}

% Supplement: Analysis_II_Script_v1.pdf, p. 164
\begin{proposition}[Product rule for the curl]
\label{prop:curl_product_rule}
Let $U \subseteq \mathbb{R}^3$ be open, $F : U \to \mathbb{R}^3$ a $C^1$ vector field, and
$\varphi : U \to \mathbb{R}$ a $C^1$ function. Then
\[\curl(\varphi F) = \varphi\,\curl(F) + (\grad\varphi)\times F.\]
\end{proposition}

\begin{proof}
By the product rule applied componentwise, e.g.\ for the first component,
\[\partial_2(\varphi F_3) - \partial_3(\varphi F_2)
  = \varphi(\partial_2F_3-\partial_3F_2) + (\partial_2\varphi)F_3 - (\partial_3\varphi)F_2,\]
and the first term is $\varphi\,\curl(F)$'s first component, the last two are
$((\grad\varphi)\times F)$'s first component. The other two components follow identically,
cyclically permuting the indices.
\end{proof}

\begin{ainote}
The official script sets this identity as an exercise (Ex.\ 14.61) but states it without the
first term, as $\curl(\varphi F) = (\grad\varphi)\times F$, which is false in general. Taking
$\varphi\equiv1$ would force $\curl F \equiv 0$ for every $F$. The missing term is exactly
$\varphi\,\curl(F)$, restored above. (The script's exercise also names the vector field $f$ in
its statement and $F$ in its conclusion, as though the two were interchangeable.)
\end{ainote}

\begin{aiexercise}[Testing a product rule against a special case]
% Generator: Claude Opus 5 (High)
\label{ex:ai_curl_product_rule_counterexample}
The identity the official script sets as an exercise, without the first term of
\cref{prop:curl_product_rule}, is
\[\curl(\varphi F) \overset{?}{=} (\grad\varphi)\times F.\]
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item Find a $\varphi$ and an $F$ for which this fails, and say what both sides evaluate to.
    The cheapest choice needs no differentiation of $\varphi$ at all.
  \item Verify \cref{prop:curl_product_rule} directly for $\varphi(x,y,z) := z$ and
    $F(x,y,z) := (-y,\,x,\,0)$, by computing $\curl(\varphi F)$, $\varphi\,\curl F$ and
    $(\grad\varphi)\times F$ separately and checking that the last two add up to the first.
\end{enumerate}
\end{aiexercise}

\begin{aiexercise}[Which fields are gradients?]
% Generator: Claude Opus 5 (High)
\label{ex:ai_which_fields_are_gradients}
On $\mathbb{R}^3$, consider
\[F(x,y,z) := (yz,\ xz,\ xy), \qquad G(x,y,z) := (y,\ -x,\ 0).\]
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item Use \cref{ex:curl_grad_div_curl_zero} to rule one of the two out as a gradient field.
  \item For the other, find a function $f$ with $\nabla f = F$.
  \item Confirm the verdict of part \textbf{(a)} by a second, independent route: compute the
    line integral of the ruled-out field along the unit circle
    $\gamma(t) := (\cos t,\ \sin t,\ 0)$, $t \in [0,2\pi]$, and compare with
    \cref{prop:work_difference_potential}.
\end{enumerate}
\end{aiexercise}


% --- exercises relocated here from the dissolved sheets ---

% Originally: content/week-11.tex, \exercisesheet{11}, problem 11.1
% Source: exercises/Ex11_Analysis2_eng.pdf

\begin{exercise}[Flow of vector field \textnormal{(important)}]
\label{ex:11.1}
Compute the flow of the vector field
\[V : (x,y,z) \mapsto (x,y,z-x^2-y^2)\]
through the upper hemisphere $S = \{(x,y,z)\in\mathbb{R}^3 : x^2+y^2+z^2=1,\ z\geq0\}$ from
\qt{inside} to \qt{outside}. That is the number
\[\int_S V(x)\cdot\nu(x)\,d\vol_2.\]
\exinfo{This exercise is Problem 11.1 of Problem Sheet 11, Analysis II, Spring Semester 2026.}
\end{exercise}

% Source: exercises/Ex11_Analysis2_eng.pdf, p. 2
