% Originally: content/week-09.tex, \subsection{Volume of embedded surfaces} --- promoted to a section

\section{Volume of embedded surfaces}
\label{sec:volume_embedded_surfaces}

% Source: Corsin Nick/Class Notes/Week 9.pdf, p. 5
% Def. 13.68
\begin{definition}[$d$-volume on a parametrized $d$-submanifold]
\label{def:d_volume_parametrized_submanifold}
Let $V \subseteq \mathbb{R}^d$ be open, and let $\phi : V \to \mathbb{R}^n$ be a parametrized
submanifold. Given a Jordan measurable set $E \subseteq V$ with $\overline E \subseteq V$,
we define the $d$-volume of $\phi(E)$ by
\[\vol_d(\phi(E)) := \int_E \sqrt{\det\big(D\phi(x)\transp D\phi(x)\big)}\,dx.\]
\end{definition}

Here the Gram determinant in the integrand is the volume of the parallelotope spanned by the
vectors $\partial_1\phi(x),\partial_2\phi(x),\dots,\partial_d\phi(x)$, which by assumption
(see \qt{parametrized submanifold}) are linearly independent, i.e.\ $D\phi(x)$ has full rank,
so the volume is strictly positive.

% Generator: Claude Opus 5 (High) --- FIG-W09-03: 3D embedded surface and Gram area element
\begin{center}
% Correction: Claude Opus 5 (High) --- every label in the right-hand panel sat directly on the
% shaded surface, the tangent plane or the green parallelogram, with nothing behind it, so the
% mesh lines ran straight through the glyphs. Legible enough at 300dpi in colour; on a grey,
% low-contrast e-reader screen it is not. The `lbl' style below puts a soft white plate behind
% the text only (text opacity=1 keeps the glyphs solid), which is the standard TikZ idiom and
% costs nothing in print. Anything drawn over a fill from here on should carry it.
\begin{tikzpicture}[>=Latex,
  lbl/.style={fill=white, fill opacity=0.78, text opacity=1, inner sep=1.2pt,
              rounded corners=1pt}]

  % --- Left Panel: Parameter domain V in R^2 ---
  \begin{scope}[xshift=-4.5cm, yshift=0cm, scale=1.15]
    \node[font=\small\bfseries, text=TextBoldColor] at (0, 2.4) {Parameter domain $V \subseteq \mathbb{R}^2$};
    
    % Coordinate axes (u, v)
    \draw[->, thick, gray!65] (-1.6,-1.0) -- (1.8,-1.0) node[right, font=\scriptsize, text=black] {$u$};
    \draw[->, thick, gray!65] (-1.6,-1.0) -- (-1.6,2.0) node[above, font=\scriptsize, text=black] {$v$};

    % Region V (shaded domain)
    \draw[TextBoldColor, fill=TextBoldColor!6, thick, dashed]
      plot[smooth cycle, tension=0.7] coordinates {(-1.2,-0.5) (1.2,-0.6) (1.4,1.3) (-0.3,1.6) (-1.3,0.7)};
    \node[TextBoldColor, font=\small\bfseries] at (-0.9, 1.1) {$V$};

    % Infinitesimal area element at (u, v)
    \coordinate (U0) at (-0.1, 0.0);
    \coordinate (U1) at (0.9, 0.0);
    \coordinate (U2) at (-0.1, 0.8);
    \coordinate (U12) at (0.9, 0.8);

    % Shaded rectangle [u, u+du] x [v, v+dv]
    \fill[orange!25, draw=orange!90!black, thick] (U0) rectangle (U12);
    \node[font=\scriptsize\bfseries, text=orange!95!black] at ($(U0)!0.5!(U12)$) {$du\,dv$};

    % Base vectors
    \draw[blue!85!black, very thick, ->] (U0) -- (U1) node[midway, below, font=\scriptsize] {$du\,e_1$};
    \draw[purple!85!black, very thick, ->] (U0) -- (U2) node[midway, left, font=\scriptsize] {$dv\,e_2$};
    \fill[black] (U0) circle (2pt) node[below left=1pt, font=\scriptsize] {$(u,v)$};

    % Coordinate grid lines passing through (u,v)
    \draw[gray!50, densely dashed] (-1.0, 0.0) -- (U0);
    \draw[gray!50, densely dashed] (U1) -- (1.2, 0.0);
    \draw[gray!50, densely dashed] (-0.1, -0.5) -- (U0);
    \draw[gray!50, densely dashed] (U2) -- (-0.1, 1.3);
  \end{scope}

  % --- Mapping arrow phi ---
  \draw[->, line width=1.4pt, gray!75] (-2.2, 0.6) to[bend left=18] 
    node[above, font=\large\bfseries, text=TextBoldColor] {$\phi$} 
    node[below, font=\tiny, text=gray!80!black] {parametrization} (-0.6, 0.6);

  % --- Right Panel: 3D Surface in R^3 with Tangent Plane & Parallelogram ---
  \begin{scope}[xshift=2.8cm, yshift=0cm, x={(-1.1cm,-0.55cm)}, y={(1.25cm,-0.15cm)}, z={(0cm,1.45cm)}]
    \node[font=\small\bfseries, text=TextBoldColor] at (0.0, 0.0, 2.2) {Embedded surface $S = \phi(V) \subseteq \mathbb{R}^3$};

    % 3D Coordinate Axes (x, y, z) at corner
    \draw[->, thick, gray!60] (-1.7, -1.7, -0.6) -- (2.0, -1.7, -0.6) node[below left, font=\scriptsize, text=black] {$x$};
    \draw[->, thick, gray!60] (-1.7, -1.7, -0.6) -- (-1.7, 2.0, -0.6) node[right, font=\scriptsize, text=black] {$y$};
    \draw[->, thick, gray!60] (-1.7, -1.7, -0.6) -- (-1.7, -1.7, 1.6) node[above, font=\scriptsize, text=black] {$z$};

    % Background shading of surface
    \fill[cyan!18, opacity=0.85]
      (-1.5,-1.5, {0.18*(-1.5)*(-1.5) - 0.12*(-1.5)*(-1.5)})
      \foreach \u in {-1.5,-1.0,...,1.5} {
        -- (\u, 1.5, {0.18*\u*\u - 0.12*1.5*1.5 + 0.08*\u})
      }
      \foreach \v in {1.5,1.0,...,-1.5} {
        -- (1.5, \v, {0.18*1.5*1.5 - 0.12*\v*\v + 0.08*1.5})
      }
      \foreach \u in {1.5,1.0,...,-1.5} {
        -- (\u, -1.5, {0.18*\u*\u - 0.12*(-1.5)*(-1.5) + 0.08*\u})
      }
      -- cycle;

    % U-curves (coordinate lines v = const)
    \foreach \v in {-1.5,-1.0,-0.5,0.0,0.5,1.0,1.5} {
      \draw[cyan!60!blue, thin]
        plot[smooth, domain=-1.5:1.5, samples=9]
        (\x, \v, {0.18*\x*\x - 0.12*\v*\v + 0.08*\x});
    }

    % V-curves (coordinate lines u = const)
    \foreach \u in {-1.5,-1.0,-0.5,0.0,0.5,1.0,1.5} {
      \draw[cyan!60!blue, thin]
        plot[smooth, domain=-1.5:1.5, samples=9]
        (\u, \x, {0.18*\u*\u - 0.12*\x*\x + 0.08*\u});
    }

    % Surface outer boundary
    \draw[TextBoldColor, thick]
      plot[smooth, domain=-1.5:1.5, samples=9] (\x, -1.5, {0.18*\x*\x - 0.12*1.5*1.5 + 0.08*\x})
      -- plot[smooth, domain=-1.5:1.5, samples=9] (1.5, \x, {0.18*1.5*1.5 - 0.12*\x*\x + 0.08*1.5})
      -- plot[smooth, domain=1.5:-1.5, samples=9] (\x, 1.5, {0.18*\x*\x - 0.12*1.5*1.5 + 0.08*\x})
      -- plot[smooth, domain=1.5:-1.5, samples=9] (-1.5, \x, {0.18*1.5*1.5 - 0.12*\x*\x - 0.08*1.5})
      -- cycle;

    % Highlight coordinate line v = 0
    \draw[blue!90!black, line width=1.5pt]
      plot[smooth, domain=-1.5:1.5, samples=11] (\x, 0, {0.18*\x*\x + 0.08*\x})
      node[lbl, below left=0pt, font=\tiny\bfseries, text=blue!90!black] {$\phi(u, v_0)$};
    
    % Highlight coordinate line u = 0
    \draw[purple!90!black, line width=1.5pt]
      plot[smooth, domain=-1.5:1.5, samples=11] (0, \x, {-0.12*\x*\x})
      node[lbl, above right=0pt, font=\tiny\bfseries, text=purple!90!black] {$\phi(u_0, v)$};

    % Point P = phi(x)
    \coordinate (P) at (0, 0, 0);

    % Tangent vectors in 3D:
    \coordinate (T1) at (1.2, 0.0, 0.10);
    \coordinate (T2) at (0.0, 1.2, 0.0);
    \coordinate (T12) at ($(T1)+(T2)-(P)$);

    % Semi-transparent Tangent Plane Patch
    \fill[gray!25, opacity=0.45] 
      (-0.6, -0.6, -0.05) -- (1.8, -0.6, 0.15) -- (1.8, 1.8, 0.15) -- (-0.6, 1.8, -0.05) -- cycle;
    \draw[gray!60, dashed, thin]
      (-0.6, -0.6, -0.05) -- (1.8, -0.6, 0.15) -- (1.8, 1.8, 0.15) -- (-0.6, 1.8, -0.05) -- cycle;
    % text=, not a bare colour: a bare colour in node options sets `color=', which
    % overrides the fill=white that `lbl' supplies and paints the plate dark.
    \node[lbl, text=gray!70!black, font=\tiny\bfseries] at (1.7, 1.7, 0.15) {$T_{\phi(x)}S$};

    % Tangent Parallelogram on the tangent plane
    \fill[ThemeGreen!35, opacity=0.85] (P) -- (T1) -- (T12) -- (T2) -- cycle;
    \draw[ThemeGreen!80!black, thick, dashed] (T1) -- (T12) -- (T2);

    % Tangent Vectors (bold arrows)
    \draw[blue!95!black, line width=1.8pt, ->] (P) -- (T1)
      node[lbl, below right=0pt, font=\scriptsize\bfseries] {$\partial_1\phi(x)\,du$};
    \draw[purple!95!black, line width=1.8pt, ->] (P) -- (T2)
      node[lbl, above left=0pt, font=\scriptsize\bfseries] {$\partial_2\phi(x)\,dv$};

    % Point P dot
    \fill[black] (P) circle (2.2pt) node[lbl, below left=2pt, font=\scriptsize\bfseries] {$\phi(x)$};

    % Normal vector
    \coordinate (N) at (0, 0, 1.1);
    \draw[orange!95!black, line width=1.4pt, ->] (P) -- (N) node[above, font=\scriptsize\bfseries] {$\mathbf{n}$};

    % Center of parallelogram for callout
    \coordinate (Center) at ($(P)!0.5!(T12)$);
  \end{scope}

  % 2D Screen Overlay Callout for Area formula
  \node[ThemeGreen!75!black, font=\small, fill=white, inner sep=3.5pt, rounded corners=3pt, draw=ThemeGreen!60, line width=0.8pt, align=center] (AreaBox) at (4.5, 2.3) 
    {\textbf{Infinitesimal Surface Area:}\\[1.5pt]
     $d\sigma = \sqrt{\det\big(D\phi(x)\transp D\phi(x)\big)}\,du\,dv$};
  
  \draw[ThemeGreen!85!black, thick, ->] (AreaBox.south west) to[out=215, in=60] (3.4, 0.4);

\end{tikzpicture}
\end{center}

This factor gives a measure for the stretching of $E$ as it is \qt{glued} to the surface. If
\[\sqrt{\det\big(D\phi(x)\transp D\phi(x)\big)} = 1 \quad \text{for all } x,\]
then $\phi$ is called an \newterm{isometry} \germanfor{Isometrie}. A special case is the
length of a curve, treated in \cref{sec:length_of_curve} below.

% Quelle: Linus Lüchinger/Slides/Slides-04-20.pdf, slide 14
% Generator: Claude Opus 5 (High)
% Feedback: Gemini 3.1 Pro (2026-08-11) --- praised for stopping the cross-product formula from
% reading as magic: the three steps derive it, and the closing paragraph recovers arc length and
% $|\partial_1\phi \times \partial_2\phi|$ as the $d=1$ and $d=2$ cases. Keep the closing
% paragraph in particular; it is what connects this to the formula the reader already uses.
\begin{remark}[Why the transpose, and why the square root]
\label{rem:why_gram_determinant}
The shape of the formula looks arbitrary until one asks what could possibly stand in its place.
The honest question is: \emph{by what factor does $\phi$ stretch $d$-dimensional volume near
$x$?} Near $x$ the map $\phi$ is its differential, so the question is really about the linear
map $D\phi(x)$, and the answer runs in three steps.
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item \textbf{The obvious candidate does not exist.} For a map $\mathbb{R}^n \to \mathbb{R}^n$
    the stretching factor is $\lvert\det\rvert$. Here $D\phi(x)$ is $n \times d$ with $d < n$, a
    \emph{rectangular} matrix, and a rectangular matrix has no determinant. So, the one-line
    answer is unavailable, and something has to replace it.
  \item \textbf{A factor nevertheless exists.} A linear map still scales all $d$-volumes in its
    domain by one and the same constant: it sends the unit cube of $\mathbb{R}^d$ to a fixed
    parallelotope, and every other region is built from scaled copies of that cube. Call this
    constant $c \geq 0$. It is what we want; we only need a formula for it.
  \item \textbf{The Gram matrix supplies it.} The product $D\phi(x)\transp D\phi(x)$ is
    $d \times d$, square at last, so it \emph{does} have a determinant, and that
    determinant turns out to be exactly $c^2$. Hence $c = \sqrt{\det(D\phi\transp D\phi)}$, and
    the square root is there for no deeper reason than to undo that squaring.
\end{enumerate}
The two familiar formulas are the small cases. For $d=1$ the matrix $D\phi(t)$ is the single
column $\phi'(t)$, so $D\phi\transp D\phi = \lvert\phi'(t)\rvert^2$ and $c = \lvert\phi'(t)\rvert$,
which is the arc-length integrand of \cref{sec:length_of_curve}. For $d=2$, $n=3$ one computes
$c = \lvert\partial_1\phi \times \partial_2\phi\rvert$, the area of the parallelogram the two
tangent vectors span, which is the surface-area formula used throughout this chapter.
\end{remark}

% Quelle: Linus Lüchinger/Slides/Slides-04-20.pdf, slide 12
\begin{remark}[Two objects, three notations]
\label{rem:notation_D_phi}
From this chapter onwards the lecture notes write $D\phi(x)$ for the Jacobian \emph{matrix} of
$\phi$ at $x$, the object earlier chapters of this document call $\Jac\phi(x)$. That collides
with the notation $D\phi_x$ used since \cref{ch:differential} for the \emph{differential}, the
linear map itself. The two are not the same kind of object, even though they carry the same
information:
\[\underbrace{D\phi_x}_{\text{linear map}}(v) \;=\; \underbrace{\Jac\phi(x)}_{\text{matrix}}\,v
  \;=\; \underbrace{D\phi(x)}_{\text{same matrix}}\,v.\]
The subscript is the whole distinction: $D\phi_x$ has $x$ downstairs, $D\phi(x)$ has it in
brackets. Both spellings of the matrix appear in the chapters ahead; where it matters, read
$D\phi(x)$ and $\Jac\phi(x)$ as synonyms.
\end{remark}

% Supplement: Sascha Brack/Class Notes/Week_09_Notes_Friday.pdf, p. 3
\begin{example}[Integral on the graph of a function]
\label{ex:integral_graph_function}
Given an open set $U \subseteq \mathbb{R}^2$ and a $C^1$ function $f: U \to \mathbb{R}$, we can define the parametrized surface $\phi: U \to \mathbb{R}^3$ by $\phi(x,y) = (x,y,f(x,y))\transp$.
Assuming $M := \operatorname{Im}\phi$ is a $C^1$ submanifold, its area is given by $\int_M 1 \mathop{d\text{vol}}_2$.

We first find the Jacobian matrix of the parametrization:
\[\Jac\phi(x,y) = \begin{pmatrix} 1 & 0 \\ 0 & 1 \\ \partial_1 f(x,y) & \partial_2 f(x,y) \end{pmatrix}.\]
Then we compute the Gram determinant:
\begin{align*}
\sqrt{|\det(\Jac\phi\transp \Jac\phi)|}
&= \sqrt{\det \begin{pmatrix} 1+(\partial_1 f)^2 & \partial_1 f \partial_2 f \\ \partial_2 f \partial_1 f & 1+(\partial_2 f)^2 \end{pmatrix}} \\
&= \sqrt{\big(1+(\partial_1 f)^2\big)\big(1+(\partial_2 f)^2\big) - (\partial_1 f)^2(\partial_2 f)^2} \\
&= \sqrt{1 + (\partial_1 f)^2 + (\partial_2 f)^2} = \sqrt{1+|\nabla f(x,y)|^2}.
\end{align*}
By definition, we get the surface area formula for a graph:
\[\int_M 1 \mathop{d\text{vol}}_2 = \int_U \sqrt{1+|\nabla f(x,y)|^2} \,dx\,dy.\]
\end{example}

\subsection{Example: area of a paraboloid patch}
\label{sec:example_paraboloid_area}

% Source: Corsin Nick/Class Notes/Week 9.pdf, pp. 6--7
\begin{example}[Area of a paraboloid patch]
\label{ex:paraboloid_patch_area}
Compute the area of the surface
\[F = \left\{(x,y,z)\in\mathbb{R}^3 \ :\ x^2+4y^2 < 8,\ \ z = \tfrac12(x^2+2y^2)\right\}.\]

Since $z = \tfrac12(x^2+2y^2)$ means $F$ is a graph, we can conveniently write $F$ as the
image of the parametrization
\[\phi : A \to F, \qquad (x,y) \mapsto \left(x,\,y,\,\tfrac12(x^2+2y^2)\right),
  \qquad A := \{(x,y)\in\mathbb{R}^2 : x^2+4y^2 < 8\}.\]

\textbf{Gram determinant.} We compute
\[D\phi(x,y) = \begin{pmatrix} 1 & 0 \\ 0 & 1 \\ x & 2y \end{pmatrix}
  \quad \Longrightarrow \quad
  D\phi(x,y)\transp D\phi(x,y) = \begin{pmatrix} 1+x^2 & 2xy \\ 2xy & 1+4y^2 \end{pmatrix},\]
so that
\[\sqrt{\det D\phi(x,y)\transp D\phi(x,y)}
  = \sqrt{(1+x^2)(1+4y^2) - 4x^2y^2}
  = \sqrt{1+x^2+4y^2}.\]

\textbf{The integral.} By \cref{def:d_volume_parametrized_submanifold},
\[\vol_2(F) = \int_A \sqrt{1+x^2+4y^2}\,dx\,dy, \qquad A = \{(x,y)\in\mathbb{R}^2 :
x^2+4y^2<8\}.\]

We should choose more convenient coordinates. Polar coordinates come to mind, but then we
still have problems, since $A$ is not radially symmetric. We want to write
$A = \Psi\big((0,R)\times(0,2\pi)\big)$ (up to a set of measure zero). Note that for $y=0$,
$|x| < \sqrt8 = 2\sqrt2$, and for $x=0$, $|y| < \sqrt2$. Choosing $R := \sqrt2$ and weighing
$x,y$ accordingly,
\[\begin{pmatrix} x \\ y \end{pmatrix} = \begin{pmatrix} 2r\cos\theta \\ r\sin\theta
\end{pmatrix} =: \Psi(r,\theta).\]

\textbf{Change of variables.}
\[dx\,dy = |\det D\Psi(r,\theta)|\,dr\,d\theta
  = \left|\det\begin{pmatrix} 2\cos\theta & -2r\sin\theta \\ \sin\theta & r\cos\theta
    \end{pmatrix}\right| dr\,d\theta = 2r\,dr\,d\theta,\]
giving
\[
\begin{aligned}
\vol_2(F) &= \int_A \sqrt{1+x^2+4y^2}\,dx\,dy
  = \int_0^{\sqrt2} dr\int_0^{2\pi} d\theta\ 2r\sqrt{1+4r^2} \\
  &= 4\pi\int_0^{\sqrt2} dr\ r(1+4r^2)^{1/2}
   = \frac\pi2\int_0^{\sqrt2} dr\ 8r(1+4r^2)^{1/2} \\
  &= \frac\pi2\left[(1+4r^2)^{3/2}\cdot\frac23\right]_0^{\sqrt2}
   = \frac\pi3\big(27-1\big) = \frac{26\pi}{3}.
\end{aligned}
\]
\end{example}

\begin{ainote}
Worth flagging what makes $A$ awkward: it is an ellipse, not a disc, so naive polar
coordinates $(x,y)=(\rho\cos\theta,\rho\sin\theta)$ would force $\rho$ to depend on $\theta$
along the boundary. The fix is to build the eccentricity into the change of variables itself.
Scaling $x$ by a factor of $2$ relative to $y$ turns the ellipse into the disc $r<\sqrt2$
before polar coordinates are even applied. This is the same idea as
\Cref{ex:change_of_variables_domain}: choose $\Psi$ to straighten the domain, not
to simplify the integrand.
\end{ainote}

% Supplement: Sascha Brack/Class Notes/Week_09_Notes_Monday.pdf, p. 7
\begin{example}[Surface area of a sphere]
\label{ex:surface_area_sphere}
Given the sphere $M := \{(x,y,z) \in \mathbb{R}^3 \mid x^2+y^2+z^2 = r^2\}$, we can use the parametrization $\phi : (0,\pi) \times (-\pi,\pi) \to \mathbb{R}^3$,
\[\phi(\theta,\varphi) = \begin{pmatrix} r\sin\theta\cos\varphi \\ r\sin\theta\sin\varphi \\ r\cos\theta \end{pmatrix}.\]
The Jacobian matrix is
\[\Jac\phi(\theta,\varphi) = \begin{pmatrix} r\cos\theta\cos\varphi & -r\sin\theta\sin\varphi \\ r\cos\theta\sin\varphi & r\sin\theta\cos\varphi \\ -r\sin\theta & 0 \end{pmatrix},\]
which has rank 2 for all $(\theta,\varphi)$ in the domain.
Then we compute the Gram determinant:
\[\Jac\phi\transp \Jac\phi = \begin{pmatrix} r^2 & 0 \\ 0 & r^2\sin^2\theta \end{pmatrix} \quad \Longrightarrow \quad \sqrt{\det(\Jac\phi\transp \Jac\phi)} = r^2\sin\theta.\]
The surface area is given by the integral:
\begin{align*}
\int_M 1 \mathop{d\text{vol}}_2 &= \int_{-\pi}^\pi \int_0^\pi \sqrt{\det(\Jac\phi\transp \Jac\phi)} \,d\theta\,d\varphi \\
&= \int_{-\pi}^\pi \int_0^\pi r^2\sin\theta \,d\theta\,d\varphi \\
&= r^2 \left(\int_{-\pi}^\pi d\varphi\right) \left(\int_0^\pi \sin\theta \,d\theta\right) \\
&= r^2 \cdot 2\pi \cdot 2 = 4\pi r^2.
\end{align*}
Note that here, our partition of unity simply was the identity (since the parametrization covers the sphere up to a null set).

\begin{center}
\begin{tikzpicture}[scale=1.5]
  % Submanifold shape
  \draw[thick] (0,0) .. controls (0.5,0.2) and (1.5,-0.2) .. (2,0)
               .. controls (2.2,-0.5) and (1.8,-0.8) .. (2,-1)
               .. controls (1.5,-0.8) and (0.5,-1.2) .. (0,-1)
               .. controls (-0.2,-0.5) and (0.2,-0.5) .. (0,0);
               
  % Two charts
  \draw[very thick, green!60!black] (0.5,-0.5) circle (0.8);
  \node[green!60!black] at (-0.3,-1.2) {$B_{r_1}(p_1)$};
  
  \draw[very thick, green!60!black] (1.5,-0.5) circle (0.8);
  \node[green!60!black] at (2.3,-1.2) {$B_{r_2}(p_2)$};
  
  % Correction: Claude Opus 5 (High) --- this block sat at (2.5,-0.3). It is eight lines, so
  % about 2.8cm tall, and `right' centres it vertically on that point: its last line came down
  % to the height of the $B_{r_2}(p_2)$ label above and overlapped it, printing "whole
  % submanifold." across the label. Node text does not follow `scale=1.5', so the block is
  % wider and taller than the coordinates suggest. Shifted right to clear the label.
  \node[right, font=\footnotesize, align=left, text=green!60!black] at (2.9, -0.3) {
    and $\eta_\ell(x)$ is defined such\\
    that when we integrate\\
    in each ball separately\\
    and add all the\\
    integrals together,\\
    we exactly get a notion\\
    of the integral on the\\
    whole submanifold.
  };
\end{tikzpicture}
\end{center}
\end{example}

% Source: exercises_2024/mock.pdf (Analysis II mock examination, Spring Semester 2024,
% Prof. Joaquim Serra), Exercise 11, p. 3
% Extractor: Claude Opus 5 (High)
% The only block in this chapter that puts def:d_volume_parametrized_submanifold to work on a map
% given by a formula. Its two halves pull in opposite directions and that is the point: Psi is
% affine and injective, so the formula is exact; Phi is two-to-one, so it is not. See the AI-Note
% after the solution -- the exam's own formula sheet states the area formula with no injectivity
% hypothesis, which makes its part (a) false as printed.
\begin{exercise}[True or False \textnormal{(two maps and the area formula)}]
\label{ex:area_formula_two_maps}
The examination this problem is taken from supplies, as a formula to be used without proof, the
statement that for $\Phi \in C^1(\mathbb{R}^n,\mathbb{R}^m)$ with $m \geq n$ and $K \subseteq
\mathbb{R}^n$ compact,
\begin{equation}
\label{eq:mock_area_formula}
  \vol_n(\Phi(K)) = \int_K \sqrt{\det\big(\Jac\Phi(x)\transp \Jac\Phi(x)\big)}\,dx .
\end{equation}
For $\alpha \in \mathbb{R}$ consider the maps
\[\Phi : (x,y) \mapsto (xy,\ x^2-y^2), \qquad
  \Psi : (x,y) \mapsto (x+y,\ \alpha x - y,\ y+1).\]
Decide whether each of the following is true or false, for every compact $E \subseteq
\mathbb{R}^2$.
\begin{enumerate}[label=\textbf{(\alph*)}]
  \item $\vol_2(\Phi(E)) = 2\int_E (x^2+y^2)\,dx\,dy$.
  \item $\vol_2(\Phi(E)) = 2\int_E (x^2+2y^2)\,dx\,dy$.
  \item $\vol_2(\Psi(E)) = 2\alpha\,\vol_2(E)$.
  \item $\vol_2(\Psi(E)) = \sqrt{2}\,\lvert\alpha+1\rvert\,\vol_2(E)$.
  \item $\vol_2(\Psi(E)) = \sqrt{2(\alpha^2+\alpha+1)}\,\vol_2(E)$.
\end{enumerate}
\exinfo{This exercise is taken from the mock examination for Analysis II, Spring Semester 2024
(Prof.\ Joaquim Serra), where it appears as the eleventh block of multiple-choice questions.
Formula~\eqref{eq:mock_area_formula} is quoted from that paper's own list of results that
candidates may use without proof.}
\exsol{sol:area_formula_two_maps}
\end{exercise}


% --- exercises relocated here from the dissolved sheet 9 ---

% Originally: content/week-09.tex, \exercisesheet{9}, problem 9.1
% Source: exercises/Ex9_Analysis2_eng.pdf

% Source: exercises/Ex9_Analysis2_eng.pdf, p. 1
\begin{exercise}[Volume \textnormal{(important)}]
\label{ex:paraboloid_hypersurface_volume}
Calculate the volume of the region $B \subset \mathbb{R}^3$ enclosed by the surfaces
$x^2+y^2+z^2=8$ and $2z=x^2+y^2$.
\exhint[Official hint]{Use cylindrical coordinates.}

% Source: Corsin Nick/Class Notes/Week 9.pdf, p. 1
% The environment form rather than \exhint, because this hint is a display.
\begin{exercisehint}[Corsin's hint]
\[\begin{pmatrix} x \\ y \\ z\end{pmatrix} = \begin{pmatrix} r\sin\theta\cos\varphi \\
  r\sin\theta\sin\varphi \\ r\cos\theta \end{pmatrix}
  \qquad \text{for } (r,\theta,\varphi) \in (0,\infty)\times(0,\pi)\times(0,2\pi).\]
\end{exercisehint}
\exinfo{This exercise is Problem 9.1 of Problem Sheet 9, Analysis II, Spring Semester 2026.}
\exsol{sol:paraboloid_hypersurface_volume}
\end{exercise}

% The solution in 99-solutions.tex points back at this note, so it carries a label. Possible
% only since 2026-08-09, when ainote gained its own counter; the solution used to say
% "see the \texttt{ainote} above", which leaked a LaTeX environment name into the typeset text.
\begin{ainote}[Corsin's hint is spherical, not cylindrical]
% Correction: Claude Opus 5 (High) --- was note:9.1_coordinates_mislabelled. The 2026-08-23
% label sweep matched only digits straight after the colon, so this one and ex:solution_9.1
% survived it; the slug now says what the note is about, as the rule requires.
\label{note:corsin_hint_spherical_not_cylindrical}
Corsin labels the formula above \qt{cylindrical coordinates}, but $(r\sin\theta\cos\varphi$, $r\sin\theta\sin\varphi$, $r\cos\theta)$ is the \textbf{spherical} parametrization, and his
range $\theta \in (-\pi/2,\pi/2)$ has been corrected to $(0,\pi)$, the range on which
$\theta \mapsto \cos\theta$ actually covers $[-1,1]$ bijectively (already logged in \cref{ex:spherical_coordinates_diffeomorphism}). The mislabelling is doubly worth noting here, since the official sheet's own hint
recommends \emph{cylindrical} coordinates $(r,\theta,z)$, and that is in fact the more
natural choice for this problem, as the solution on \cpageref{sol:paraboloid_hypersurface_volume}
does: both surfaces are
surfaces of revolution about the $z$-axis, which cylindrical coordinates respect directly,
whereas spherical coordinates mix $r$ and $\theta$ into both defining equations.
\end{ainote}

% Originally: content/week-10.tex, \exercisesheet{10}, problem 10.1
% Source: exercises/Ex10_Analysis2_eng.pdf

% Source: exercises/Ex10_Analysis2_eng.pdf, p. 1
\begin{exercise}[Spherical quadrilateral \textnormal{(important)}]
\label{ex:spherical_quadrilateral_area}
Calculate the area of the spherical quadrilateral $S \subset \mathbb{S}^2$ bounded by the
meridians $-\pi < \varphi_1 < \varphi_2 < \pi$ and parallels $-\pi/2 < \theta_1 < \theta_2 <
\pi/2$.
\exinfo{This exercise is Problem 10.1 of Problem Sheet 10, Analysis II, Spring Semester 2026.}
\exsol{sol:spherical_quadrilateral_area}
\end{exercise}

% Originally: content/week-10.tex, \exercisesheet{10}, problem 10.2
% Source: exercises/Ex10_Analysis2_eng.pdf

% Source: exercises/Ex10_Analysis2_eng.pdf, p. 1
\begin{exercise}[The surface area of a torus \textnormal{(important)}]
\label{ex:torus_surface_area}
Let $S := \{(x,y,0)\in\mathbb{R}^3 \mid x^2+y^2=4\}$ and $T := \{x\in\mathbb{R}^3 \mid d_S(x)
\leq 1\}$, where $d_S(x) = \inf\{\lvert x-y\rvert \mid y\in S\}$ denotes the minimal distance
from $S$ to $x\in\mathbb{R}^3$. Parametrize $\partial T$ and then calculate the area.

% Source: Corsin Nick/Class Notes/Week 10.pdf, p. 1
% The environment form rather than \exhint, because this hint carries a display.
\begin{exercisehint}[Corsin's hint, general parametrization of a torus surface]
\[x = (R+r\cos\phi)\cos\theta, \qquad y = (R+r\cos\phi)\sin\theta, \qquad z = r\sin\phi,\]
for the torus swept by a tube of radius $r$ at distance $R$ from the axis.
\end{exercisehint}

% Sonnet 5 (Medium) --- FIG-W10-01
\begin{center}
\begin{tikzpicture}[>=Latex, line join=round, line cap=round, scale=1.0]
  % --- 1. 3D view ---
  \begin{scope}
    \node[font=\small\bfseries, text=TextBoldColor] at (0, 2.8) {3D view};
    
    % Torus background half
    \draw[cyan!30!blue, thick, fill=cyan!8, opacity=0.9] (0,0) ellipse (2.0 and 0.8);
    % Inner hole
    \draw[cyan!30!blue, thick, fill=white, opacity=1.0] (0,0) ellipse (0.8 and 0.3);
    
    % Axes (drawn on top)
    \draw[->, thick, gray!60] (0,0) -- (2.5, 0) node[right, font=\scriptsize, text=black] {$x$};
    \draw[->, thick, gray!60] (0,0) -- (0, 1.8) node[above, font=\scriptsize, text=black] {$z$};
    \draw[->, thick, gray!60] (0,0) -- (-1.4, -1.0) node[left, font=\scriptsize, text=black] {$y$};

    % Draw some cross sections to give 3D feel
    \draw[cyan!60!blue, thick, dashed] (1.4, 0) ellipse (0.6 and 0.5);
    \draw[cyan!60!blue, thick, densely dotted] (-1.4, 0) ellipse (0.6 and 0.5);
    
    % Torus front half (highlighted)
    \draw[blue!90!black, thick] (-2.0,0) arc (180:360:2.0 and 0.8);
    \draw[blue!90!black, thick] (-0.8,0) arc (180:360:0.8 and 0.3);
    
    % R and r in 3D
    \draw[->, red!85!black, thick] (0,0) -- (1.4, 0) node[pos=0.6, below, font=\scriptsize] {$R$};
    \draw[->, orange!95!black, thick] (1.4,0) -- (1.4, 0.5) node[pos=0.7, right=-1pt, font=\scriptsize] {$r$};
    \fill[black] (1.4,0) circle (1.5pt);
  \end{scope}

  % --- 2. Top View (xy-plane) ---
  \begin{scope}[xshift=5.5cm]
    \node[font=\small\bfseries, text=TextBoldColor] at (0, 2.8) {Top view ($xy$-plane)};
    
    \fill[cyan!8, opacity=0.9] (0,0) circle (2.0);
    \fill[white] (0,0) circle (0.8);
    
    \draw[blue!90!black, thick] (0,0) circle (2.0);
    \draw[blue!90!black, thick] (0,0) circle (0.8);
    
    % Axes (drawn on top)
    \draw[->, thick, gray!60] (0,0) -- (2.6, 0) node[right, font=\scriptsize, text=black] {$x$};
    \draw[->, thick, gray!60] (0,0) -- (0, 2.3) node[above, font=\scriptsize, text=black] {$y$};

    % Draw central circle R
    \draw[gray, dashed, thick] (0,0) circle (1.4);
    
    \coordinate (O) at (0,0);
    \coordinate (CenterT) at (40:1.4);
    \coordinate (OuterT) at (40:2.0);
    
    \draw[->, red!85!black, thick] (O) -- (CenterT) node[pos=0.55, above left=-2pt, font=\scriptsize] {$R$};
    \draw[->, orange!95!black, thick] (CenterT) -- (OuterT) node[pos=0.6, below right=-2pt, font=\scriptsize] {$r$};
    
    % Angle theta
    \draw[gray!80!black, thick] (0.6,0) arc (0:40:0.6);
    \node[font=\scriptsize, text=black] at (20:0.8) {$\theta$};
  \end{scope}

  % --- 3. Cross section view (rz-plane) ---
  % Correction: Claude Opus 5 (High) --- was xshift=10.5cm with the axis running to 2.4, which
  % put the $x,y$ label past the text block: the one Overfull \hbox in the document (13.99pt,
  % reported against this picture's closing lines). Panel pulled in and its axis shortened;
  % 2.1 still clears the tube circle, whose right edge is at 2.0.
  \begin{scope}[xshift=10.0cm]
    \node[font=\small\bfseries, text=TextBoldColor] at (0, 2.8) {Cross-section};
    
    \coordinate (C) at (1.4, 0);
    \fill[cyan!8, opacity=0.9] (C) circle (0.6);
    \draw[blue!90!black, thick] (C) circle (0.6);
    
    % Axes (drawn on top)
    \draw[->, thick, gray!60] (-1.5, 0) -- (2.1, 0) node[right, font=\scriptsize, text=black] {$x,y$};
    \draw[->, thick, gray!60] (0, -1.2) -- (0, 1.8) node[above, font=\scriptsize, text=black] {$z$};

    \draw[->, orange!95!black, thick] (C) -- ++(50:0.6) node[pos=0.7, above left=-2pt, font=\scriptsize] {$r$};
    
    \draw[gray!80!black, thick] (C) ++(0.3,0) arc (0:50:0.3);
    \node[font=\scriptsize, text=black] at ($(C) + (25:0.45)$) {$\phi$};
    
    % Draw R to center
    % Correction: Claude Opus 5 (high) --- was `ThemeRed', a colour this document never
    % defines; pgfkeys errored out and the arrow printed black, breaking the colour match
    % with the $R$ arrow of the Top view panel above. Same tint as there.
    \draw[->, red!85!black, thick] (0, -0.25) -- (1.4, -0.25) node[midway, below, font=\scriptsize] {$R$};
    \draw[gray, dotted, thick] (0, 0) -- (1.4, 0);
    \draw[gray, dotted, thick] (1.4, 0) -- (1.4, -0.3);
  \end{scope}
\end{tikzpicture}
\end{center}
\exinfo{This exercise is Problem 10.2 of Problem Sheet 10, Analysis II, Spring Semester 2026.}
\exsol{sol:torus_surface_area}
\end{exercise}
