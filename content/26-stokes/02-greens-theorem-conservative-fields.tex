% Originally: content/week-11.tex, \subsection{Green's theorem and conservative fields} --- promoted to a section

\section{Green's theorem and conservative fields}
\label{sec:greens_theorem_conservative_fields}

\begin{ainote}
Green's theorem, conservative vector fields, and path integrals were also discussed in the Monday session. 
\end{ainote}

\subsection{Line integrals (Wegintegrale)}

\begin{definition}[Work / Line integral]
\label{def:line_integral}
Let $U \subseteq \mathbb{R}^n$ be an open set, $F : U \to \mathbb{R}^n$ a continuous vector field, and $\gamma : [a,b] \to U$ a piecewise $C^1$ path. We define the \newterm{work} (or \newterm{line integral}) of $F$ along $\gamma$ as
\[\int_\gamma F \cdot d\gamma := \int_a^b F(\gamma(t)) \cdot \gamma'(t) \,dt.\]
The work measures how much the vector field \qt{pushes} along the path.
\end{definition}

\subsection{Green's theorem}

\begin{theorem}[Green's Theorem]
\label{thm:greens_theorem}
Let $F : U \to \mathbb{R}^2$ be a continuously differentiable vector field on an open set $U \subseteq \mathbb{R}^2$. Then, for any compact $C^1$ domain $\Omega \subseteq U$,
\[\int_\Omega \curl F \mathop{d\text{vol}}_2 = \int_{\partial\Omega} F \cdot \tau \,dL,\]
where $\tau$ is the unit tangent vector to the boundary $\partial\Omega$ oriented counter-clockwise (i.e.\ $\tau = (-\nu_2, \nu_1)\transp$, with $\nu$ being the outward unit normal).
\end{theorem}

\begin{ainote}
% Supplement: Sascha Brack/Class Notes/Week_10_Notes_Monday.pdf, p. 16
% Redrawn 2026-08-09 (Opus 5, high). Five faults in the version it replaces:
%   * THE MATHEMATICAL ONE. Both blue arrows on the shared edge were backwards. Traversed
%     counterclockwise, the left cell's right edge runs UP and the right cell's left edge runs
%     DOWN. The figure drew the downward arrow at x = -0.05, on the left cell's side, and the
%     upward one at x = +0.05, on the right cell's side, so each cell's shared edge contradicted
%     that cell's own orientation. Since the pair is still antiparallel the picture appeared to
%     cancel, which is why this survived: the cancellation is the conclusion, but which arrow
%     belongs to which cell is the reason for it, and that was the part reversed.
%   * Every direction arrow was a degenerate segment of length 0.01, e.g.
%     `(-0.25,1.0) -- (-0.26,1.0)`, drawn only to hang an arrowhead on. At scale 1.5 that is
%     0.015cm, so they rendered as malformed barbs, and the two blue ones, 0.1 apart, merged
%     into one unreadable blob.
%   * The picture set no `>=Latex`, alone among the Part VII figures.
%   * The domain circle was drawn twice, black underneath and red arcs on top, leaving a black
%     fringe along the boundary.
%   * `cancels` and `out` were two nodes at two unrelated positions, so they read as two
%     separate labels, and the leader landed on a cell corner rather than on the shared edge.
% The interior circulations are now drawn with a 30-degree gap, which is what makes their sense
% of rotation legible at this size, and they run counterclockwise like the boundary --- the
% agreement is the whole content of the picture.
\begin{center}
\begin{tikzpicture}[>=Latex, scale=1.5]
  % The domain, oriented counterclockwise as the theorem requires.
  \draw[red, ultra thick] (0,0) circle (1.5);
  \foreach \a in {45,135,225,315} {
    \draw[red, ultra thick, ->] ({1.5*cos(\a-7)},{1.5*sin(\a-7)}) arc (\a-7:\a+7:1.5);
  }
  \node[red, align=left, font=\small] at (1.55,1.75) {boundary\\is left over};
  \draw[red, ->] (0.98,1.56) to[out=205, in=75] (0.44,1.43);

  % Interior circulations, counterclockwise like the boundary. Each is a 330-degree arc centred
  % on (\x,\y): it starts at 120 degrees, at (\x-0.1, \y+0.1732), and ends at 450 = 90 degrees,
  % i.e. at the TOP of the circle, where the counterclockwise tangent is exactly (-1,0). Putting
  % the head there and nowhere else is deliberate: a horizontal arrowhead is unambiguous at this
  % size, and it lets the reader compare it directly against the boundary arrowheads, which is
  % the whole content of the picture. An earlier version ended the arc at 330 degrees, where the
  % head sits at 5 o'clock at a slant and the sense of rotation cannot be read off at all.
  \foreach \x/\y in {-0.6/-0.3, 0.4/-0.4, -0.15/-0.75, 0.8/-0.8} {
    \draw[red, thick, ->] ({\x+0.2*cos(120)},{\y+0.2*sin(120)}) arc (120:450:0.2);
  }

  % Two adjacent cells, each traversed counterclockwise.
  \draw[red, thick] (-0.75,0.55) rectangle (-0.25,0.95);
  \draw[red, thick] (-0.25,0.55) rectangle (0.25,0.95);
  % The left cell's outer edges.
  \draw[red, thick, ->] (-0.59,0.55) -- (-0.41,0.55);
  \draw[red, thick, ->] (-0.41,0.95) -- (-0.59,0.95);
  \draw[red, thick, ->] (-0.75,0.84) -- (-0.75,0.66);
  % The right cell's outer edges.
  \draw[red, thick, ->] (-0.09,0.55) -- (0.09,0.55);
  \draw[red, thick, ->] (0.09,0.95) -- (-0.09,0.95);
  \draw[red, thick, ->] (0.25,0.66) -- (0.25,0.84);
  % The shared edge: up for the left cell, down for the right one.
  \draw[blue, thick, ->] (-0.34,0.63) -- (-0.34,0.87);
  \draw[blue, thick, ->] (-0.16,0.87) -- (-0.16,0.63);

  \node[purple, font=\scriptsize, align=center] at (0.45,0.14)
    {these cancel\\on the shared edge};
  \draw[purple, ->] (-0.25,0.30) -- (-0.25,0.52);
\end{tikzpicture}
\end{center}
We integrate the rotation inside the domain. Basically all the \qt{intermediate values} cancel out because for two points close to each other, the rotation points in opposite directions. Only the boundary is left over in the end!
\end{ainote}

\begin{example}[Area of a region via Green's theorem]
\label{ex:area_greens_theorem}
Consider a region $\Omega \subseteq \mathbb{R}^2$ defined by the region inside the curve $x^{2/3} + y^{2/3} = 1$ in the first quadrant, bounded by the axes. The curve can be parametrized as $\varphi(t) = (\cos^3 t, \sin^3 t)\transp$ for $t \in [0, \pi/2]$.

The area is $\vol_2(\Omega) = \int_\Omega 1 \mathop{d\text{vol}}_2$. We look for a vector field $F$ such that $\curl F = \partial_x F_2 - \partial_y F_1 \equiv 1$ on $\Omega$. A simple choice is $F(x,y) := (0, x)\transp$.
By Green's theorem, we have:
\[\vol_2(\Omega) = \int_\Omega \curl F \mathop{d\text{vol}}_2 = \int_{\partial\Omega} F \cdot \tau \,dL = \int_0^{\pi/2} F(\varphi(t)) \cdot \varphi'(t) \,dt.\]
Since $\tau(\varphi(t)) \,dL = \varphi'(t) \,dt$ along the parametrization, we compute the dot product:
\[F(\varphi(t)) \cdot \varphi'(t) = \begin{pmatrix} 0 \\ \varphi_1(t) \end{pmatrix} \cdot \begin{pmatrix} \varphi_1'(t) \\ \varphi_2'(t) \end{pmatrix} = \varphi_1(t)\varphi_2'(t).\]
This turns the area computation into a single 1D integral in $t$.
\end{example}

\begin{example}[Line integral via Green's theorem]
\label{ex:line_integral_greens}
We want to compute the work of $F(x,y) := (0, xy^2)\transp$ along the ellipse $\gamma$ given by $x^2 + (y/2)^2 = 1$, oriented counter-clockwise. (This is Michael's Bsp.\ 18.1.1(b)).
Let $\Omega = \{(x,y) \in \mathbb{R}^2 \mid x^2 + y^2/4 \leq 1\}$. By Green's theorem,
\[\int_\gamma F \cdot d\gamma = \int_{\partial\Omega} F \cdot \tau \,dL = \int_\Omega \curl F \mathop{d\text{vol}}_2.\]
We have $\curl F = \partial_x(xy^2) - \partial_y(0) = y^2$. We compute the double integral using modified polar coordinates $x = r \cos \theta$, $y = 2r \sin \theta$ with Jacobian $2r$:
\[\int_\Omega y^2 \mathop{d\text{vol}}_2 = \int_0^{2\pi} \int_0^1 (2r \sin\theta)^2 (2r) \,dr\,d\theta = 8 \int_0^{2\pi} \sin^2\theta \,d\theta \int_0^1 r^3 \,dr = 8 (\pi) \left(\frac{1}{4}\right) = 2\pi.\]
\end{example}

\begin{example}[Line integral of a curl-free field]
\label{ex:curl_free_greens}
Let $\Omega$ be the unit disk $B_1(0) \subseteq \mathbb{R}^2$ and $F(x,y) := (xy^2, x^2y)\transp$. We compute $\int_{\partial\Omega} F \cdot \tau \,dL$ (Michael's Bsp.\ 20.2.1).
By Green's theorem, we need to integrate the curl of $F$ over $\Omega$:
\[\curl F = \partial_x(x^2y) - \partial_y(xy^2) = 2xy - 2xy = 0.\]
Thus, the integral is simply $\int_\Omega 0 \mathop{d\text{vol}}_2 = 0$.
\end{example}

\begin{example}[2D Divergence theorem]
\label{ex:2d_divergence_theorem}
Green's theorem can also be viewed as a 2D version of the divergence theorem. Consider the vector field $F(x,y) := (x - y^2, y - x^2)\transp$. We want to compute the outward flux of $F$ through the curve $\gamma$ given by the graph of $y = \cos x$ for $x \in [-\pi/2, \pi/2]$ (Michael's Bsp.\ 19.6.2).

We can close the curve by adding the segment $\sigma$ on the $x$-axis from $\pi/2$ to $-\pi/2$, forming a closed region $\Omega$ (the area under the cosine bump). By the 2D divergence theorem:
\[\int_{\gamma} F \cdot \nu \,dL + \int_{\sigma} F \cdot \nu \,dL = \int_\Omega \divg F \mathop{d\text{vol}}_2.\]
The divergence is $\divg F = \partial_x(x-y^2) + \partial_y(y-x^2) = 1 + 1 = 2$.
The integral of the divergence is $2 \vol_2(\Omega) = 2 \int_{-\pi/2}^{\pi/2} \cos x \,dx = 2(2) = 4$.
For the segment $\sigma(x) = (x, 0)$ from $\pi/2$ to $-\pi/2$, the outward normal from $\Omega$ is $\nu = (0, -1)\transp$. The flux through $\sigma$ is:
\[\int_{\sigma} F \cdot \nu \,dL = \int_{-\pi/2}^{\pi/2} \begin{pmatrix} x \\ -x^2 \end{pmatrix} \cdot \begin{pmatrix} 0 \\ -1 \end{pmatrix} \,dx = \int_{-\pi/2}^{\pi/2} x^2 \,dx = \left[ \frac{x^3}{3} \right]_{-\pi/2}^{\pi/2} = \frac{\pi^3}{12}.\]
Therefore, the flux through $\gamma$ is $4 - \frac{\pi^3}{12}$.
\end{example}

% Supplement: Analysis_II_Script_v1.pdf, p. 152
\begin{example}[The work of a field can depend on the path]
\label{ex:path_dependence_three_paths}
Let $F(x,y) := (-y,x)\transp$, and let $\gamma_0,\gamma_1,\gamma_2 : [0,1] \to \mathbb{R}^2$ be
the three paths from $(0,0)$ to $(1,1)$ given by $\gamma_0(t) := (t,t)$ (the diagonal), and
\[\gamma_1(t) := \begin{cases}(2t,0) & t\in[0,\tfrac12]\\ (1,2t{-}1) & t\in[\tfrac12,1]\end{cases}
  \qquad
  \gamma_2(t) := \begin{cases}(0,2t) & t\in[0,\tfrac12]\\ (2t{-}1,1) & t\in[\tfrac12,1]\end{cases}\]
(right-then-up, and up-then-right, along the sides of the unit square). Direct computation
gives three different answers:
\[\int_{\gamma_0}F\cdot d\gamma_0 = \int_0^1\langle(-t,t),(1,1)\rangle\,dt = 0, \qquad
  \int_{\gamma_1}F\cdot d\gamma_1 = 1, \qquad \int_{\gamma_2}F\cdot d\gamma_2 = -1.\]
Moving \emph{perpendicular} to $F$ costs no work, moving \emph{with} $F$ does positive work, and
moving \emph{against} $F$ does negative work. Since the three paths share endpoints but
disagree on the work done, $F$ cannot be conservative (\cref{def:conservative_field} below):
\Cref{prop:work_difference_potential} would otherwise force all three integrals to agree.
\end{example}

\subsection{Conservative vector fields}

\begin{definition}[Conservative vector field]
\label{def:conservative_field}
Given a $C^k$ vector field $F$ on an open set $U \subseteq \mathbb{R}^n$, we call it \newterm{conservative} if there exists a scalar field $f : U \to \mathbb{R}$ of class $C^{k+1}$ such that $F \equiv \nabla f$. The function $f$ is called a \newterm{potential} for $F$.
\end{definition}

\begin{lemma}[Integrability conditions]
\label{lem:integrability_conditions}
Let $U \subseteq \mathbb{R}^n$ be open, and let $F$ be a $C^1$ conservative vector field on $U$ with components $F_1, \dots, F_n$. Then we necessarily have the integrability conditions:
\[\partial_j F_k = \partial_k F_j \qquad \text{for all } j, k \in \{1,\dots,n\}.\]
\end{lemma}
\begin{proof}
Let $f$ be a potential such that $F = \nabla f = (\partial_1 f, \dots, \partial_n f)\transp$. Then by Schwarz's theorem (symmetry of second derivatives),
\[\partial_j F_k = \partial_j(\partial_k f) = \partial_j\partial_k f = \partial_k\partial_j f = \partial_k(\partial_j f) = \partial_k F_j.\qedhere\]
\end{proof}

\begin{example}[A non-conservative field]
\label{ex:non_conservative_field}
Not every vector field is conservative. For example, a frictional force is not conservative. Mathematically, consider $F : \mathbb{R}^3 \to \mathbb{R}^3$ given by $F(x,y,z) := (xz-y, x^2+z^3, 3xz^3 - xy)\transp$. If $F$ were conservative, it would have to satisfy the integrability conditions (\cref{lem:integrability_conditions}). But
\[\partial_x F_2 = 2x \quad \text{and} \quad \partial_y F_1 = -1.\]
Since they are not equal, $F$ cannot be conservative.
\end{example}

\begin{proposition}[Work along a path as difference of potential]
\label{prop:work_difference_potential}
Let $U \subseteq \mathbb{R}^n$ be open, and let $F : U \to \mathbb{R}^n$ be a continuous conservative vector field with potential $f$. Then for any $C^1$ path $\gamma : [0,1] \to U$, the line integral of $F$ along $\gamma$ depends only on the endpoints:
\[\int_\gamma F \cdot d\gamma = f(\gamma(1)) - f(\gamma(0)).\]
\end{proposition}
\begin{proof}
By definition of the path integral and the chain rule,
\begin{align*}
\int_\gamma F \cdot d\gamma &= \int_0^1 F(\gamma(t)) \cdot \gamma'(t) \,dt = \int_0^1 \nabla f(\gamma(t)) \cdot \gamma'(t) \,dt \\
&= \int_0^1 Jf(\gamma(t)) J\gamma(t) \,dt = \int_0^1 J(f \circ \gamma)(t) \,dt = \int_0^1 (f \circ \gamma)'(t) \,dt.
\end{align*}
By the fundamental theorem of calculus, this equals $(f \circ \gamma)(1) - (f \circ \gamma)(0) = f(\gamma(1)) - f(\gamma(0))$.
\end{proof}

\begin{example}[Line integral of a conservative field]
\label{ex:conservative_field_integral}
Consider the vector field $F : [0,1] \times [0,2] \to \mathbb{R}^2$ given by $F(x,y) := (y^2, 2xy)\transp$.
First, we show that $F$ is conservative by finding a potential $f$ such that $\nabla f = F$. We need $\partial_x f = y^2$ and $\partial_y f = 2xy$. Integrating the first equation with respect to $x$ gives $f(x,y) = xy^2 + c(y)$. Differentiating with respect to $y$ gives $\partial_y f = 2xy + c'(y)$, which must equal $2xy$. Thus $c'(y) = 0$, and we can choose $c(y) = 0$. The potential is $f(x,y) = xy^2$.

Now, let $\Gamma$ be a closed path on the boundary $\partial U$ of the domain, starting and ending at $\gamma(0) = \gamma(1) = (0,0)\transp$. By \cref{prop:work_difference_potential}, the line integral around this closed loop is simply the difference in potential at the endpoints:
\[\oint_\Gamma F \cdot d\gamma = f(\gamma(1)) - f(\gamma(0)) = f(0,0) - f(0,0) = 0.\]
\end{example}

\begin{example}[Line integral of a conservative field via potential]
\label{ex:line_integral_potential}
Consider the vector field $F(x,y) := (10x^4 - 2xy^3, -3x^2y^2)\transp$ and a path $\gamma$ connecting $(0,0)$ to $(2,1)$, for instance, the curve defined by $x^4 - 6xy^3 = 4y^2$ (Michael's Bsp.\ 18.2.7).

Since $F$ is defined on all of $\mathbb{R}^2$ and satisfies $\partial_y F_1 = -6xy^2 = \partial_x F_2$, it is conservative. We find a potential $f(x,y)$ by integration:
\begin{align*}
\partial_x f &= 10x^4 - 2xy^3 \implies f(x,y) = 2x^5 - x^2y^3 + c(y), \\
\partial_y f &= -3x^2y^2 + c'(y).
\end{align*}
Equating this with $F_2 = -3x^2y^2$, we get $c'(y) = 0$, so we can choose $c(y) = 0$. The potential is $f(x,y) = 2x^5 - x^2y^3$.
By \cref{prop:work_difference_potential}, the work along $\gamma$ is path-independent and given by:
\[\int_\gamma F \cdot d\gamma = f(2,1) - f(0,0) = (2(32) - 4(1)) - 0 = 60.\]
Notice that the equation of the curve $x^4 - 6xy^3 = 4y^2$ was irrelevant, as long as it connects the two endpoints!
\end{example}

% Supplement: Analysis_II_Script_v1.pdf, p. 156
\begin{exercise}[Which $\alpha$ makes this field conservative?]
\label{ex:find_alpha_conservative}
For which $\alpha \in \mathbb{R}$ is the vector field
$F(x,y) := (\alpha xe^y,\ (y+1+x^2)e^y)\transp$ conservative? Find a potential for that value of
$\alpha$.
\end{exercise}

\begin{exercisesolution}[Proof of \cref{ex:find_alpha_conservative}]
% Generator: Claude Sonnet 5 (High)
By \cref{lem:integrability_conditions}, a necessary condition is
$\partial_yF_1 = \partial_xF_2$, i.e.\ $\alpha xe^y = 2xe^y$ for all $(x,y)$, forcing
$\alpha = 2$. (This is also sufficient, since $F$ is $C^1$ on all of $\mathbb{R}^2$, which is
simply connected, so a potential exists.) With $\alpha=2$,
$F(x,y) = (2xe^y,\ (y+1+x^2)e^y)\transp$; integrating $\partial_xf=2xe^y$ gives
$f(x,y) = x^2e^y+c(y)$, and matching $\partial_yf = x^2e^y+c'(y)$ against
$F_2=(x^2+y+1)e^y$ gives $c'(y)=(y+1)e^y$, so $c(y) = ye^y$ (check:
$\tfrac{d}{dy}(ye^y)=e^y+ye^y=(y+1)e^y$). A potential is $f(x,y) = x^2e^y+ye^y$.
\end{exercisesolution}

% Supplement: Analysis_II_Script_v1.pdf, p. 164
\begin{exercise}[Choosing parameters for an irrotational field]
\label{ex:choose_parameters_irrotational}
Determine real parameters $\alpha,\beta,\gamma$ such that the vector field
\[F(x,y,z) := (x+2y+\alpha z,\ \beta x-3y-z,\ 4x+\gamma y+2z)\]
on $\mathbb{R}^3$ becomes irrotational, i.e.\ $\curl(F)=0$. Determine, after choosing these
parameters, a potential for $F$.
\end{exercise}

\begin{exercisesolution}[Proof of \cref{ex:choose_parameters_irrotational}]
% Generator: Claude Sonnet 5 (High)
Setting $\curl F = 0$ componentwise (\cref{def:rotation}):
\[\partial_yF_3-\partial_zF_2 = \gamma-(-1) \overset{!}{=} 0, \qquad
  \partial_zF_1-\partial_xF_3 = \alpha-4 \overset{!}{=} 0, \qquad
  \partial_xF_2-\partial_yF_1 = \beta-2 \overset{!}{=} 0,\]
giving $\gamma=-1$, $\alpha=4$, $\beta=2$, so
$F(x,y,z) = (x+2y+4z,\ 2x-3y-z,\ 4x-y+2z)$. This $F$ is $C^1$ on the simply connected
$\mathbb{R}^3$ and curl-free, hence conservative (the $3$-dimensional case of
\cref{cor:potentials_simply_connected}). Integrating $\partial_xf = x+2y+4z$ gives
$f = \tfrac12x^2+2xy+4xz+c(y,z)$; matching $\partial_yf = 2x+c_y$ against $F_2=2x-3y-z$ gives
$c_y=-3y-z$, so $c = -\tfrac32y^2-yz+d(z)$; matching
$\partial_zf = 4x-y+d'(z)$ against $F_3=4x-y+2z$ gives $d'(z)=2z$, so $d(z)=z^2$. A potential is
\[f(x,y,z) = \tfrac12x^2+2xy+4xz-\tfrac32y^2-yz+z^2.\]
\end{exercisesolution}
